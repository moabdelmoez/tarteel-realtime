# Session Handoff

## Verified Now

- Latest slice: selected-Surah-only Apple recitation UI, completed locally on 2026-06-14 in the repo root on `main`.
- Physical-iPhone Auto-mode diagnosis used `/private/tmp/tarteel-iphone-auto-20260614.logarchive`: CoreML loaded and emitted transcripts, but the app connected without `scope=`, then full-corpus matching produced multi-second no-match locator windows, including `locator_ms=32564.0` after disconnect.
- iPhone and macOS no longer expose the Auto/Surah segmented picker. The visible recitation control is now only the Surah picker.
- `RecitationViewModel` defaults to `.selectedSurah`, rewrites stale `.autoDetect` preferences to `.selectedSurah`, coerces explicit `selectRecitationMode(.autoDetect)` calls back to selected Surah, and always builds recording URLs with `.selectedSurah(id: selectedSurahID)`.
- Verification passed: red Swift/Python guardrails failed on the old Auto behavior first; full Swift client core passed with 42 XCTest plus 92 Swift Testing tests; Apple source guardrails passed with 30 Python tests; iOS Simulator `TarteelPrototypeCoreMLReplay` build passed; macOS `TarteelPrototypeMac` build passed; feature-list JSON, `git diff --check`, compileall, and full Python with 319 tests passed.
- Next manual step: install/open the rebuilt physical-iPhone app, choose the intended Surah, recite from the beginning or a middle ayah, and confirm unified logs show `coreml_asr_connect ... scope=<surah-id>` plus bounded selected-Surah locator timings.
- Latest slice: macOS app build repair after extracting `CoreMLLocalQuranSession`, completed locally on 2026-06-13 in the repo root on `main`.
- User build log first showed stale/corrupt derived data: FluidAudio `MachTaskSelfWrapper` failed because a Darwin PCM under `/private/tmp/tarteel-xcode-derived-macos` was missing, and the compile command still referenced `.worktrees/coreml-fastconformer-spike`.
- Fresh derived data exposed the actual project issue: `CoreMLFastConformerClient.swift` could not find `CoreMLLocalQuranSession` because the hand-authored Xcode project did not include the new extracted file in app target source phases.
- Fixed `ios/TarteelPrototype/TarteelPrototype.xcodeproj/project.pbxproj` by adding `CoreMLLocalQuranSession.swift` to the Shared Core group and both iPhone/macOS source build phases.
- Strengthened `tests.test_macos_app_project` so the shared-core project guardrail enumerates every Swift file under `ios/TarteelClientCore/Sources/TarteelClientCore`, preventing future extracted source files from silently missing app targets.
- Verification passed: focused macOS project guardrails with 16 tests, fresh-derived-data macOS build, original-derived-data `clean build`, and original-derived-data normal macOS build.
- Next manual step: open `/private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app` and run the CoreML capture/replay test.
- Latest slice: CoreML local reason vocabulary deepening, completed locally on 2026-06-13 in the repo root on `main`.
- Added internal `CoreMLLocalQuranEventReason` so `CoreMLLocalQuranSession` carries typed reason cases through matches and transcript construction, then emits the same raw wire strings only at `RecitationEvent` construction.
- Updated local-session tests to use the reason vocabulary and added `localQuranEventReasonsKeepWireContractValues` to pin every current raw `coreml_local_*` value in one contract table.
- Verification passed: focused `CoreMLFastConformerTests` with 53 Swift Testing tests and full Swift client core with 34 XCTest plus 86 Swift Testing tests. No iOS/macOS app build or physical-device run was performed for this internal architecture-only slice.
- Next architecture step: consider splitting corpus loading from match policy only if it improves locality or test leverage; event mapping should probably stay behind the session seam until there is a second caller.
- Latest slice: architecture review plus behavior-preserving `CoreMLLocalQuranSession` extraction, completed locally on 2026-06-13 in the repo root on `main`.
- Generated architecture report: `/var/folders/b1/_3ms4wf1765fjddl0nb083580000gn/T/architecture-review-20260613T180635.html`. The recommended first move was to deepen the Apple local Quran session seam before changing its interface.
- Added `CoreMLLocalQuranSession` to `CONTEXT.md` as the Apple local recitation-location seam that maps cumulative CoreML transcripts to `SessionEvent`-compatible events.
- Moved `CoreMLLocalQuranSession`, the local Quran corpus/types, matching helpers, and local `RecitationEvent` constructors out of `CoreMLFastConformerClient.swift` into `ios/TarteelClientCore/Sources/TarteelClientCore/CoreMLLocalQuranSession.swift`. No matcher thresholds, reason strings, scope policy, or event payload fields were intentionally changed.
- Verification passed before/after the extraction: focused `CoreMLFastConformerTests` with 52 Swift Testing tests, full Swift client core with 34 XCTest plus 85 Swift Testing tests, `uv run python -B -m json.tool feature_list.json /tmp/tarteel-feature-list.validated.json`, and `git diff --check`. No iOS/macOS app build or physical-device run was performed for this architecture-only slice.
- Next architecture step: use the new file boundary to reduce `CoreMLLocalQuranSession` interface coupling, especially reason-string ownership and whether corpus loading, matching policy, and event mapping should split under focused tests.
- Latest slice: Modal-only Provider Settings UI, completed locally on 2026-06-13 in `.worktrees/modal-fastconformer-quran-ar` on branch `codex/modal-fastconformer-quran-ar`.
- iPhone `SettingsSheet` and macOS `MacSettingsView` no longer expose Generic or RunPod in the Custom Provider control. Settings now shows a fixed Provider `Modal` row, keeps the Modal ASR model picker, and uses Modal-specific bearer-token labeling.
- `BackendProvider.generic` and `.runPod` remain in the shared Swift code for compatibility with saved preferences, URL normalization, token storage, and existing regression coverage; the Settings surface migrates any old saved Custom provider to Modal via `selectModalCustomBackendProviderForSettings()`.
- Verification passed: Apple source guardrails with 23 Python tests, full Swift client core with 40 XCTest tests plus 91 Swift Testing tests, iOS Simulator `TarteelPrototypeCoreMLReplay` build, macOS `TarteelPrototypeMac` build, and `git diff --check`.
- Next manual step: reopen the rebuilt macOS app and confirm Settings shows Provider `Modal` with no Generic/RunPod dropdown, then use the ASR model picker for NeMo/Faster Whisper Modal checks.

- Latest slice: macOS Modal pasted-bearer-token normalization fix, completed locally on 2026-06-13 in `.worktrees/modal-fastconformer-quran-ar` on branch `codex/modal-fastconformer-quran-ar`.
- The Modal URL shown in the screenshot is valid; the remaining failure was consistent with an auth rejection. A focused Swift repro showed that pasting `Bearer <token>` made the app send `Authorization: Bearer Bearer <token>`, and pasting `Authorization: Bearer <token>` persisted the whole header.
- `RecitationViewModel` now canonicalizes the Settings token before both socket connection and storage, so raw token, `Bearer <token>`, and `Authorization: Bearer <token>` all become the raw token.
- Verification passed: focused Swift auth regressions with 4 tests, server bearer auth tests with 3 Python tests, full Swift client core with 39 XCTest tests plus 91 Swift Testing tests, `git diff --check`, and macOS app build into `/private/tmp/tarteel-xcode-derived-macos-modal-model-picker`. The rebuilt app was opened from that bundle.
- If the rebuilt app still shows `Modal rejected...`, the entered raw token does not match the deployed Modal secret `TARTEEL_WS_BEARER_TOKEN`; URL/model routing is not the cause.
- Next manual step: paste either the raw token or the full `Bearer ...` header in macOS Settings, keep Backend `Custom`, Provider `Modal`, URL `wss://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run/ws/recitation`, select the ASR model, and start recording.

- Latest slice: macOS Modal auth UX fix, completed locally on 2026-06-13 in `.worktrees/modal-fastconformer-quran-ar` on branch `codex/modal-fastconformer-quran-ar`.
- The screenshot error `There was a bad response from the server.` was reproduced as Modal rejecting an unauthenticated WebSocket handshake with HTTP 403. The deployed app was otherwise healthy via `/ping`.
- Custom + Modal now refuses to start recording until a bearer token is present, shows `Enter the Modal bearer token in Settings before recording.`, and does not open the socket or start audio in that blocked state.
- If Modal still rejects the handshake with a bad-response/server-rejected/403-shaped error, the app now shows `Modal rejected the WebSocket request. Check the Modal bearer token in Settings and try again.`
- Verification passed: focused Swift regression filter for the missing-token and bad-response guidance cases with 2 tests, `git diff --check`, and macOS `TarteelPrototypeMac` build into `/private/tmp/tarteel-xcode-derived-macos-modal-model-picker`. The rebuilt app was opened from that DerivedData bundle.
- Next manual step: use macOS Settings with Backend `Custom`, Provider `Modal`, the deployed WSS URL, the Modal bearer token, and the desired ASR model, then start recording.

- Latest slice: Modal ASR model picker, completed locally on 2026-06-13 in `.worktrees/modal-fastconformer-quran-ar` on branch `codex/modal-fastconformer-quran-ar`.
- The Apple settings UI now shows a Modal-only `ASR model` picker when Backend is `Custom` and Provider is `Modal`; choices are `FastConformer Quran AR (NeMo)` / `nemo-fastconformer-quran-ar` and `Faster Whisper Base AR Quran` / `faster-whisper-base-ar-quran`.
- `modalASRModel` persists across app launches, defaults to NeMo, is exposed through `RecitationViewModel.selectModalASRModel(...)`, and is disabled in the iPhone/macOS settings UI while recording.
- Custom Modal recording URLs now append or replace `asr_model` while preserving `scope` and other query items, for example `wss://.../ws/recitation?scope=108&asr_model=nemo-fastconformer-quran-ar`; non-Modal providers remove stale `asr_model`.
- Modal backend wiring now accepts only the two approved slugs, defaults missing `asr_model` to NeMo, closes unknown slugs with code `1008`, installs both optional runtimes, prewarms both Hugging Face snapshots, and creates one lazy shared recognizer per slug with fresh buffered recognizers per WebSocket connection.
- `tarteel_realtime.replay_probe` now supports `--asr-model <slug>` and `--bearer-token-env MODAL_TOKEN` for manual Modal proof without URL hand-editing.
- Verification passed: focused Python model-picker suite with 52 tests, broader Python plan suite with 86 tests, full Python suite with 319 tests, Swift client core with 35 XCTest plus 91 Swift Testing tests, iOS Simulator app build, macOS app build, compileall, feature_list JSON validation, active-feature sanity `[]`, and `git diff --check`.
- Live Modal proof was not rerun after this slice. Existing live evidence still covers the NeMo path only; redeploy and replay both slugs before claiming the Faster Whisper Modal profile works live.
- Next manual step: deploy/prewarm the two-profile Modal app, then run `replay_probe` for both `--asr-model nemo-fastconformer-quran-ar` and `--asr-model faster-whisper-base-ar-quran`, followed by macOS/iOS picker checks.

- Latest slice: Modal NeMo FastConformer Quran AR worktree, completed locally on 2026-06-13 in `.worktrees/modal-fastconformer-quran-ar` on branch `codex/modal-fastconformer-quran-ar`.
- Added optional NeMo backend support for `mohammed/fastconformer-quran-ar` with nested checkpoint restore via `TARTEEL_NEMO_MODEL_FILE=phase3_full/phase3_full_wer0.0014.nemo`; heavy NeMo/Hugging Face dependencies remain opt-in and are not default project dependencies.
- Modal deploy is live at `https://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run` with WebSocket `wss://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run/ws/recitation`; `/ping` returned `{"status":"ok"}` and Modal logs showed `EncDecHybridRNNTCTCBPEModel` restored from `phase3_full/phase3_full_wer0.0014.nemo` on L4.
- App-cadence replay evidence with `--chunk-ms 160 --send-speech-end --scope 108`: `108001.wav` locked `108:1` after 57 events, `108002.wav` locked `108:2` after 38 events, and `108003.wav` locked `108:3` after 50 events.
- Manual macOS app replay against the Modal WSS URL succeeded for `108001.wav`; backend logs showed the terminal empty `speech_end` chunk flushed the buffer and emitted `event_type=locked reason=unique_match ayah_ref=108:1`.
- iOS Simulator manual proof remains open: `TarteelPrototypeCoreMLReplay` builds, but local Simulator install/launch commands hung and the captured screen was blank. Do not claim successful iOS manual Modal ASR evidence from this slice.
- Verification passed: focused Python ASR/runtime/WebSocket checks with 64 tests, full Python suite with 312 tests, compileall for `deploy tarteel_realtime tests`, Swift client core with 34 XCTest plus 88 Swift Testing tests, macOS app build, and iOS Simulator app build.
- Next manual step: repeat iOS Modal replay on a healthy Simulator session or physical device, then collect live microphone timing/cost evidence before treating Modal as more than a scoped fixture proof.
- Latest slice: selected-Surah CoreML long-current-ayah tail-skip fallback for the fresh physical-iPhone Surah 47:2 -> 47:3 failure, completed locally on 2026-06-13 in the repo root on `main`.
- Latest physical-iPhone diagnosis from `/private/tmp/tarteel-iphone-coreml-20260613-150653.logarchive`: the 15:00 selected-Surah 47 run locked `47:1`, completed ayah 1, progressed through most of `47:2`, reached `next_expected_ref=47:2:14`, then stayed `coreml_local_ordered_no_match` while ASR emitted recognizable `47:3` anchors. Audio/VAD and inference were healthy; no invalid model output or quran-logo warning appeared in the fresh run.
- Added selected-Surah-only `coreml_local_next_ayah_tail_skip_progress` after all current-ayah matchers fail. It considers only the immediate next ayah, only when the current ayah is long, at least 70% complete, with at most six words left, and the next ayah has strong ordered anchors near its start.
- New red/green Swift coverage: `localQuranSessionAdvancesToNextAyahWhenLongCurrentAyahTailIsSkipped` first failed as `locating/coreml_local_ordered_no_match`, then passed with `ayahRef=47:3`, `startRef=47:3:1`, `nextExpectedRef=47:3:7`, and `consumedWords=6`.
- Verification passed: focused Surah 47 regression; `CoreMLFastConformerTests` with 52 Swift Testing tests including existing no-premature-107:3 guardrails; full Swift client core with 34 XCTest plus 85 Swift Testing tests; iOS Simulator `TarteelPrototypeCoreMLReplay` build into `/private/tmp/tarteel-xcode-derived-tail-skip`.
- Next manual step: rebuild/open on the physical iPhone and repeat selected Surah 47. Expect late `47:2` to advance into `47:3` once clear `47:3` anchors appear, while ordinary partial-current cases remain current-ayah gated.
- Latest slice: physical-iPhone CoreML log follow-up plus `quran_logo` asset-catalog fix, completed locally on 2026-06-13 in the repo root on `main`.
- Latest physical-iPhone diagnosis from `/private/tmp/tarteel-iphone-coreml-20260613-143519.logarchive`: the June 13 selected-Surah 59 session loaded the CoreML model and 6,236-ayah Tanzil corpus, emitted 242 model windows and 163 transcript/locator events, reached `ayah_ref=59:10` with `next_expected_ref=59:11:1`, and had zero locator windows over 1s. Inference stayed around 85 ms/window and there were no app crashes, Jetsam events, or `coreml_asr_invalid_output` markers.
- The older June 12 backlog signature was locator-side, not inference-side: 4 locator windows were over 1s with max `locator_ms` around 4774.6. Current Swift timing guardrails for bounded initial matching and bounded post-lock noisy transcripts pass, so no new locator relaxation was added.
- Fixed the remaining SwiftUI `quran_logo` runtime warning by adding `Assets.xcassets/quran_logo.imageset/quran_logo.png` while keeping the existing loose bundle resource. `tests.test_macos_app_project` now guards the named-image asset-catalog entry.
- Verification passed: focused red/green `quran_logo.imageset` guardrail; `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 21 tests; focused CoreML locator timing Swift tests; full Swift client core with 34 XCTest plus 84 Swift Testing tests; iOS Simulator `TarteelPrototypeCoreMLReplay` build; macOS `TarteelPrototypeMac` build after sandbox/package-cache escalation.
- Next manual step: install/open the app on the physical iPhone and confirm the `quran_logo` warning is gone, then continue longer selected-Surah physical tests for ASR quality, especially Surah 104 and post-59:10 progression.
- Latest slice: selected-Surah CoreML located-ayah UI visibility, completed locally on 2026-06-12 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest physical-iPhone diagnosis after the final-word fix: the fresh session did reach Surah 80 ayah 4. The logs showed `coreml_asr_transcript` with `الذِّكْرَى`, `coreml_asr_locator_event type=progress ayah_ref=80:4 start_ref=80:4:4 next_expected_ref=80:5:1`, and `coreml_asr_latency_ui_event state_changed=true` at chunk 104. The remaining symptom is visibility: the user may not notice the short-ayah transition while later noisy ASR/no-match packets continue.
- Shared reducer/UI change: progress events now show a prominent `Ayah <ref>` headline; follow-up locating/no-match and buffering packets preserve that located headline when it is `Ayah <ref>` or `Locked on <ref>`; `debugNextExpectedText` exposes `next_expected_ref` to UI and share diagnostics.
- iPhone and macOS status panels now say `Latest ayah` and `Next expected`, with iPhone still keeping raw transcript off the main surface. The macOS share summary includes `Next expected`.
- New red/green coverage: `progressEventExposesLatestAyahAndNextExpectedDiagnostics` and `postProgressLocatingKeepsLocatedAyahHeadlineVisible` first failed because `debugNextExpectedText` was missing / headline was overwritten, then passed. Python source guardrails first failed before `Latest ayah` / `Next expected` labels existed.
- Verification passed: focused Swift state tests with 13 Swift Testing tests; full Swift client core with 34 XCTest plus 84 Swift Testing tests; focused Apple source guardrails with 23 tests; `git diff --check`; macOS `TarteelPrototypeMac` xcodebuild into `/private/tmp/tarteel-xcode-derived-macos-ui-status`; iOS Simulator `TarteelPrototypeCoreMLReplay` xcodebuild into `/private/tmp/tarteel-xcode-derived-ios-ui-status`.
- Next manual step: run another physical-iPhone selected Surah 80 test and confirm the visible screen shows stable `Ayah 80:4`, `Latest ayah 80:4`, and `Next expected 80:5:1` when logs emit the `80:4` progress event. The remaining long gap before `80:5` is ASR quality/decoder-preprocessing, not VAD.
- Latest slice: selected-Surah CoreML final-word post-lock recovery for physical-iPhone Surah 80 ayah 4, completed locally on 2026-06-12 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest physical-iPhone diagnosis: the fused-opening startup fix worked and the session reached `next_expected_ref=80:4:1`. It did not progress to `80:4` because ASR preserved mainly the distinctive final word `الذكرى`, while the existing short-ayah suffix fallback required two suffix words.
- Added selected-Surah-only `coreml_local_short_ayah_final_word_progress`, restricted to post-lock current expected ayah, `nextExpectedRef` word 1, exactly four expected words, a sufficiently long final word, and high final-word similarity. It advances `80:4` from `start_ref=80:4:4` to `next_expected_ref=80:5:1` without reopening broad relocking.
- New red/green Swift coverage: `localQuranSessionRecoversSurah80Ayah4FromDistinctiveFinalWord` first failed as `locating/coreml_local_ordered_no_match`, then passed with `ayahRef=80:4`, `startRef=80:4:4`, `nextExpectedRef=80:5:1`, and `consumedWords=1`.
- Verification passed: focused final-word regression; `CoreMLFastConformerTests` with 51 Swift Testing tests; full Swift client core with 34 XCTest plus 82 Swift Testing tests.
- Next manual step: rebuild/open the physical iPhone app and retest selected Surah 80. Expect startup `coreml_local_opening_fused_lock` at `80:1`, then final-word recovery at `80:4` if the ASR again only preserves `الذكرى`; Surah 104 remains an ASR blank/decoder-quality investigation.
- Latest slice: selected-Surah CoreML fused-opening startup lock for physical-iPhone Surah 80, completed locally on 2026-06-12 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest physical-iPhone diagnosis: CoreML runs on-device with full Tanzil loaded and fast inference; Surah 59 tracked strongly through early ayahs, while Surah 80 failed at startup because ASR fused/sparsified basmala plus first-ayah text (`الرَّحْمَبَسَتَوَلَّى دًاَزَّكَّ`) and Surah 104 still produced mostly blank/unusable ASR.
- Added a selected-Surah-only `coreml_local_opening_fused_lock` fallback for short first ayahs that begin with basmala. It is restricted to the first ayah, requires compact opening/basmala evidence plus first-ayah content evidence, and preserves the no-hidden-advance/current-ayah-gated progression policy.
- The first implementation was too broad and full CoreML tests caught an unsafe Surah 18 startup lock; the final version caps this fallback to short first ayahs so longer openings remain on the bounded locating path.
- New red/green Swift coverage: `localQuranSessionLocksFusedPhysicalIPhoneSurah80Opening` first failed as `locating/coreml_local_no_match`, then passed with `ayahRef=80:1`, `startRef=80:1:1`, and `nextExpectedRef=80:2:1`.
- Verification passed: focused fused-opening regression; `CoreMLFastConformerTests` with 50 Swift Testing tests; full Swift client core with 34 XCTest plus 81 Swift Testing tests; focused Apple/CoreML Python guardrails with 26 tests after restoring the shared replay scheme launch-argument value state; macOS `TarteelPrototypeMac` build; and iOS Simulator `TarteelPrototypeCoreMLReplay` build.
- Next manual step: run a fresh physical iPhone selected-Surah test for Surah 80 and Surah 104. For Surah 80, expect `reason=coreml_local_opening_fused_lock ayah_ref=80:1 next_expected_ref=80:2:1`; for Surah 104, use audio-window/blank/stream-reset/transcript logs to decide whether the next fix is decoder/preprocessing rather than locator matching.
- Latest slice: selected-Surah CoreML ayah-completion gate, completed locally on 2026-06-12 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest diagnosis: the ASR/manual result was much better, but the locator could still fabricate progress. A partial `107:2` could be followed by clear `107:3` evidence and progress to `107:3` before `107:2` was complete. A minimized Surah 49 startup also showed sequence-anchor locking displayed `49:1` while internally setting `nextExpectedRef=49:4:1`.
- Selected-Surah post-lock matching is now current-ayah gated: after lock, ordered/tolerant/anchor/forward matching searches the current expected ayah only and rejects later-ayah evidence until the current ayah completes.
- Initial prefix-span and sequence-anchor locks no longer carry hidden multi-ayah advancement. They display the start ayah and set `nextExpectedRef` after that displayed ayah, not after the approximate span end.
- Removed the local CoreML `nextExpectedRefOverride` match path and bounded post-lock transcript words so long cumulative ASR cannot recreate multi-second locator backlog. Short-ayah recent-suffix recovery remains available for cases such as Surah 18:3.
- New red/green Swift coverage: Surah 107 cannot jump from partial `107:2` to `107:3`, Surah 107 can recover when the missing `107:2` tail appears after a rejected premature next ayah, Surah 49 sequence-anchor no longer jumps to `49:4:1`, and a long noisy post-lock transcript stays under the locator latency guard.
- Verification passed: focused regressions; `CoreMLFastConformerTests` with 49 Swift Testing tests; full Swift client core with 34 XCTest plus 80 Swift Testing tests; focused Apple/CoreML Python guardrails with 26 tests; macOS `TarteelPrototypeMac` build; iOS Simulator `TarteelPrototypeCoreMLReplay` build after sequential fresh-derived-data rerun due a parallel FluidAudio/Darwin `.pcm` cache race; full Python with 298 tests; compileall; `feature_list.json` validation; `git diff --check`; debug-marker grep with no matches.
- Next manual step: launch the rebuilt macOS app, test selected Surahs 107, 49, and 87, and inspect `coreml_asr_locator_event` plus `coreml_asr_latency_engine`. A successful run should show no `107:2 -> 107:3` before `107:2` completes, no hidden initial `49:4:1` sequence-anchor jump, current-only post-lock candidate refs, and low `locator_ms`.
- Latest slice: bounded selected-Surah CoreML startup and no-skip post-lock progression, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest latency logs showed the app/CoreML route was not frozen, but selected-Surah no-lock locator work could backlog for seconds when long cumulative transcript text triggered broad fuzzy scans across the selected Surah. Latest Surah 107 logs also showed a correctness failure where the locator locked `107:1` then jumped to `107:3` / later refs from noisy cumulative evidence.
- Selected-Surah pre-lock now takes a bounded path: prefix-span over the opening ayahs, cheap exact span matching for clean late starts, then capped fuzzy/sequence-anchor/anchor checks over small opening windows. It no longer falls through to whole-selected-Surah broad fuzzy matching before the first lock.
- Post-lock forward progression is capped to the current and next ayah. The Surah 107 and Surah 80 regressions now stay on current/next candidate refs instead of jumping to later ayahs merely because later text appears in the cumulative ASR suffix.
- New red/green Swift coverage: Surah 107 opening does not jump to `107:3` / later ayahs, noisy Surah 80 cumulative text does not skip beyond the next ayah, and synthetic long Surah 18 startup stays under the 250 ms locator guard after previously taking about 24.3 seconds.
- Verification passed: focused long-startup regression around 0.096 seconds; `CoreMLFastConformerTests` with 45 Swift Testing tests; full Swift client core with 34 XCTest plus 76 Swift Testing tests; focused Apple/CoreML Python guardrails with 26 tests; macOS `TarteelPrototypeMac` build; iOS Simulator `TarteelPrototypeCoreMLReplay` build after the expected sandbox/cache rerun; full Python with 298 tests; compileall; `feature_list.json` validation; `git diff --check`; source/test-only debug grep with no matches.
- Next manual step: launch the rebuilt macOS app, select Surah 107 or 18, recite with `--tarteel-capture-audio`, and inspect `coreml_asr_latency_engine` plus `coreml_asr_locator_event`. A successful live run should show bounded `locator_ms`, candidate refs limited to current/next after lock, and no `107:1 -> 107:3` style skip.
- Latest slice: CoreML end-to-end latency telemetry, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest latency diagnosis showed the ASR is streaming in fixed 17,920-sample / ~1.12s CoreML windows, but visible transcript can feel slow because early windows may be blank or insufficient for locator lock while inference itself is fast.
- Added `AudioChunkLatencyTrace`, carried only for the CoreML preset and intentionally omitted from `AudioChunkPayload` JSON encoding so remote WebSocket payloads stay unchanged.
- New unified log markers:
  - `coreml_asr_latency_client_chunk`: app queue delay, VAD time, socket/send time, total chunk time, sample rate/bytes, and VAD metadata.
  - `coreml_asr_latency_model_window`: first/last live-chunk age inside the fixed 1,120 ms model window, last-VAD-to-window time, inference time, token count, and emitted-text flag.
  - `coreml_asr_latency_engine`: transcriber time, locator time, total engine time, event type, and reason.
  - `coreml_asr_latency_ui_event`: reducer/UI state application time and whether state changed.
- Verification passed: red Swift latency tests failed before the trace/window summary existed; focused `CoreMLFastConformerTests` passed with 43 Swift Testing tests; focused `RecitationViewModelTests/testCoreMLAudioChunksCarryLocalLatencyTraceOnly` passed; full Swift client core passed with 34 XCTest plus 74 Swift Testing tests; `tests.test_macos_app_project` passed with 16 Python tests; macOS `TarteelPrototypeMac` build passed; iOS Simulator `TarteelPrototypeCoreMLReplay` build passed after an expected sandbox/cache rerun with `/private/tmp` module caches; feature-list JSON, active-feature sanity, `git diff --check`, source/test-only debug grep, full Python with 298 tests, and compileall passed. The stale `TarteelPrototype` iOS scheme command failed because only `TarteelPrototypeCoreMLReplay` and `TarteelPrototypeMac` are shared schemes in this branch.
- Next manual step: launch the rebuilt macOS app and retest selected-Surah mic recitation. Inspect `coreml_asr_latency_client_chunk`, `coreml_asr_latency_model_window`, `coreml_asr_latency_engine`, and `coreml_asr_latency_ui_event` beside `coreml_asr_audio_window`, `coreml_asr_transcript`, and `coreml_asr_locator_event` to identify the dominant visible-delay source.
- Latest slice: CoreML ASR stream reset on speech boundary / post-transcript blank streak, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest Surah 104 macOS logs showed the user's "ASR hangs after the first couple ayahs" symptom as ASR output starvation, not an app/CoreML inference freeze: inference windows continued with normal timing, but repeated high-confidence blanks arrived during speech-level RMS/VAD-active windows.
- Added `CoreMLFastConformerStreamResetPolicy`: it requests an acoustic stream reset after VAD `speech_end` followed by active speech (`speech_boundary`) or after three active-speech blank windows after the stream has already emitted transcript text (`blank_streak`).
- The transcriber reset clears only `previousTokenID`, `cacheLastChannel`, `cacheLastTime`, and `cacheLastChannelLength`. It preserves buffered audio, cumulative transcript, processed window count, and the Swift local Quran session/locator state.
- New unified log marker: `coreml_asr_stream_reset reason=<speech_boundary|blank_streak> chunk_sequence=<...> window_index=<...> blank_streak=<...> rms=<...> vad_speech_chunks=<...> vad_probability=<...>`.
- New Swift coverage proves speech-boundary resets, post-transcript blank-streak resets, no reset for quiet blanks, and no reset for startup active blanks before the first transcript. The startup guard was added after full Swift initially caught a regression in the known-good `108001.wav` app replay.
- Verification passed: focused reset-policy tests, `CoreMLFastConformerTests` with 39 Swift Testing tests, real `108001.wav` RecitationViewModel CoreML replay, full Swift client core with 33 XCTest plus 71 Swift Testing tests, focused Apple/CoreML Python guardrails with 26 tests, macOS `TarteelPrototypeMac` build, iOS Simulator `TarteelPrototypeCoreMLReplay` build, full Python with 298 tests, compileall, feature-list JSON, active-feature sanity, source-only debug grep, and `git diff --check`.
- Next manual step: launch the rebuilt macOS app, repeat the failing long/ayah-repeat scenario, and inspect `coreml_asr_stream_reset` together with `coreml_asr_audio_window`, `coreml_asr_blank`, `coreml_asr_transcript`, and `coreml_asr_locator_event`. A successful run should recover transcript output after the reset without losing selected-Surah locator progress.
- Latest slice: selected-Surah CoreML unresolved-istiaza startup guard plus sparse first-ayah recovery, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest Surah 18 macOS logs showed full Tanzil loading, healthy speech windows, ASR-recognized istiaza, likely missed basmala in blank windows, then noisy ayah 1 fragments. The old startup matcher let that unresolved preface fall through into broad selected-Surah sequence-anchor locking, which falsely jumped to a later ayah at low confidence.
- Added an opening-preface guard: if the selected Surah first ayah starts with basmala and ASR has istiaza but no valid basmala/content lock yet, the session stays `locating` with `coreml_local_opening_preface_no_match` instead of broad pre-lock jumps.
- Added `coreml_local_opening_sparse_content_lock`, which strips istiaza, searches only first-ayah content after basmala, and locks only when at least three strong ordered anchors are present. The log-shaped Surah 18 regression now recovers to `ayah_ref=18:1`, `start_ref=18:1:9`, `next_expected_ref=18:2:1`.
- Verification passed: focused Surah 18 regression, focused istiaza group with 4 tests, `CoreMLFastConformerTests` with 36 Swift Testing tests, full Swift client core with 33 XCTest plus 67 Swift Testing tests, focused Apple/CoreML Python guardrails with 26 tests, macOS `TarteelPrototypeMac` build, and iOS Simulator `TarteelPrototypeCoreMLReplay` build.
- Next manual step: launch the rebuilt macOS app, select Surah 18, recite istiaza then basmala/content, and confirm logs no longer jump to later ayahs before the first real lock. Expect `coreml_local_opening_preface_no_match` while opening evidence is insufficient, then `coreml_local_opening_sparse_content_lock` or the existing basmala/content lock once enough first-ayah evidence appears.
- Latest slice: selected-Surah CoreML istiaza/basmala startup handling, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest Surah 18 macOS logs showed full Tanzil loading and healthy speech windows, but pre-lock locator events stayed `coreml_local_no_match` after istiaza-shaped startup noise and partial basmala fragments. Because first lock never happened, ordered progression never activated.
- Root cause: the initial selected-Surah matcher treated the whole cumulative startup transcript as an ayah candidate. It did not explicitly ignore istiaza, and `بالله` from istiaza could also confuse basmala/content start selection.
- Added selected-Surah opening-preface handling for Tanzil-style first ayahs beginning with `بسم الله الرحمن الرحيم`: `coreml_local_opening_basmala_lock` locks on a contiguous basmala suffix and sets `next_expected_ref` after basmala; `coreml_local_opening_content_lock` locks directly at post-basmala first-ayah content when basmala is skipped.
- New Swift regressions first failed: the partial-basmala case stayed `locating/coreml_local_no_match`, and the skipped-basmala case locked at the wrong start ref through broad anchor matching. After the fix, istiaza alone remains unlocked, partial basmala locks to `18:1` with `start_ref=18:1:3` / `next_expected_ref=18:1:5`, and skipped basmala locks to `18:1:5`.
- Verification passed: focused istiaza regressions, full `CoreMLFastConformerTests` with 35 Swift Testing tests, full Swift client core with 33 XCTest plus 66 Swift Testing tests, macOS `TarteelPrototypeMac` build, and iOS Simulator `TarteelPrototypeCoreMLReplay` build.
- Next manual step: launch the rebuilt macOS app, select Surah 18, recite istiaza then basmala/content, and confirm logs show `coreml_local_opening_basmala_lock` or `coreml_local_opening_content_lock` before later ordered progress.
- Latest slice: selected-Surah CoreML short-ayah suffix recovery for Surah 18, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest Surah 18 macOS logs showed full Tanzil loading and good ordered progress through `18:1` and `18:2`, ending at `next_expected_ref=18:3:1`.
- Root cause: canonical `18:3` is only `ماكثين فيه أبدا`; ASR missed `ماكثين`, emitted only weak suffix evidence such as `فِي` then `أبٍ`, and later produced blanks/noisy fragments before ayah 4. VAD was not directly gating the route, but the ASR did not provide enough anchors for the old matcher.
- Added selected-Surah-only `coreml_local_short_ayah_suffix_progress`, limited to current/next ordered ayahs with at most four words and requiring at least a two-word suffix. It accepts short prefix-shaped suffix words such as `في` for `فيه` and `أب` for `أبدا`.
- New Swift regression first failed as `locating/coreml_local_ordered_no_match`; after the fix, `فِي` alone remains no-match, while `فِي أبٍ` progresses to `ayah_ref=18:3`, `start_ref=18:3:2`, and `next_expected_ref=18:4:1`.
- Verification passed: focused Surah 18 regression, focused CoreML Swift tests with 32 Swift Testing tests, and focused Apple/CoreML Python guardrails with 26 tests.
- Next manual step: rebuild/open the macOS app, retest selected Surah 18, and confirm logs show `reason=coreml_local_short_ayah_suffix_progress ayah_ref=18:3 start_ref=18:3:2 next_expected_ref=18:4:1` when ASR emits `في أب`-shaped suffix evidence. Launch with `--tarteel-capture-audio` if possible so the exact mic audio can be replayed.
- Latest slice: CoreML live mic capture/replay diagnostics, completed locally on 2026-06-11 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `--tarteel-capture-audio <wav>` to both iPhone and macOS app entrypoints. The normal microphone streamer is wrapped by `CapturingAudioStreamer`, which writes exactly forwarded mono 16 kHz PCM16 chunks to a replayable WAV while still sending those chunks into the existing app queue, VAD metadata processing, CoreML route, and locator path.
- Captures log `coreml_asr_audio_capture_started`, `coreml_asr_audio_capture_finished`, and `coreml_asr_audio_capture_failed`; the captured WAV can be replayed with the existing `--tarteel-replay-audio <wav> --tarteel-replay-surah <id>` launch path.
- New regressions cover WAV replayability, exact forwarded chunk capture, repeated recording to the same output path, launch-argument parsing, and app entrypoint/log-marker wiring. The repeat-capture test first failed because a second recording reused the previous finished writer; after the fix, each recording reopens/overwrites cleanly.
- Verification passed: focused CoreML Swift tests with 31 Swift Testing tests, focused Apple/CoreML Python guardrails with 26 tests, full Swift client core with 33 XCTest plus 62 Swift Testing tests, full Python with 298 tests, compileall, feature-list JSON, active-feature sanity `[]`, `git diff --check`, macOS `TarteelPrototypeMac` build, and iOS Simulator `TarteelPrototypeCoreMLReplay` build.
- Next manual step: launch the macOS app executable with `--tarteel-capture-audio /private/tmp/tarteel-live-surah59.wav`, recite the failing selected Surah, stop recording so the WAV finalizes, then relaunch with `--tarteel-replay-audio /private/tmp/tarteel-live-surah59.wav --tarteel-replay-surah 59` and compare capture/audio-window/transcript/locator logs.
- Latest slice: selected-Surah CoreML Surah 59 post-lock ordered-anchor ayah-boundary recovery, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest macOS log diagnosis showed healthy audio windows and VAD speech observations, so the bad result was not caused by silent mic input or VAD gating. The locator locked/progressed through `59:1`, then failed after `next_expected_ref=59:2:1` because ASR omitted `هو الذي` and resumed with `أخرج الذين ك`.
- Added selected-Surah-only current/next ayah ordered-anchor progress after normal ordered matching and before rolling forward fallback. It accepts ordered content-word anchors and one trailing prefix token, emitting `coreml_local_ordered_anchor_progress`.
- New regression from the exact log-shaped Surah 59 stream first failed with `locating/coreml_local_ordered_no_match`; after the fix it recovers to `ayah_ref=59:2`, `start_ref=59:2:3`, `next_expected_ref=59:2:6`, `consumed_words=3`.
- Verification passed: focused red/green regression, `swift test --filter CoreMLFastConformerTests` with 27 Swift Testing tests, full Swift client core with 33 XCTest plus 58 Swift Testing tests, focused Apple/CoreML Python guardrails with 25 tests, feature-list JSON validation, active-feature sanity `[]`, `git diff --check`, full Python with 297 tests, compileall, macOS `TarteelPrototypeMac` build, and iOS simulator `TarteelPrototypeCoreMLReplay` build.
- Next manual step: relaunch `/private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app`, test selected Surah 59, and confirm logs show `reason=coreml_local_ordered_anchor_progress ayah_ref=59:2 start_ref=59:2:3` after ayah 1.
- Latest slice: CoreML ASR audio-window diagnostics, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest manual-log diagnosis: the local CoreML route is receiving regular `2,560` sample chunks and producing fixed `17,920` sample model windows, but the ASR stream after initial lock becomes sparse/noisy with many high-confidence blanks. Source inspection confirms local CoreML does not use VAD to drop audio; VAD is attached as payload metadata only.
- Added `CoreMLFastConformerAudioWindowMetrics` and a VAD-preserving buffered segment path so each model window logs `coreml_asr_audio_window` before prediction with `sample_count`, `rms`, `peak`, `near_silence_ratio`, `vad_probability`, `vad_speech_chunks`, `vad_observations`, and `vad_event`.
- This slice is diagnostic only. It does not change the CoreML model contract, does not gate on VAD, and does not alter locator behavior.
- Verification passed: red Swift metric test and red Python log-marker guardrail failed first; focused CoreML tests passed with 26 Swift Testing tests; focused Apple/CoreML guardrails passed with 25 Python tests; full Swift client core passed with 33 XCTest plus 57 Swift Testing tests; feature-list JSON, active-feature sanity, diff check, source/test debug-grep, full Python with 297 tests, compileall, macOS `TarteelPrototypeMac` build, and iOS simulator `TarteelPrototypeCoreMLReplay` build all passed.
- Next manual step: launch `/private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app`, recite selected Surah 80 or 83, and compare `coreml_asr_audio_window` against `coreml_asr_blank`, `coreml_asr_transcript`, and `coreml_asr_locator_event`. Low RMS/peak or high near-silence means mic/preprocessing/gain; healthy levels with blanks means decoder/model/preprocessing quality.
- Latest slice: selected-Surah CoreML Surah 80 sequence-anchor first-lock recovery, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Latest macOS logs showed `scope=80`, full bundled Tanzil `ayahs=6236`, correct `2,560`/`17,920` chunk cadence, but no lock. The relevant cumulative transcript was `َّ عَدَسََبَ تَوَلَّىَهُ يُدْرِيكَ لَعَلَّهُ يَزَّىكَّ`, followed by `coreml_local_no_match`.
- This was not the previous stale post-lock `80:2` problem. The ASR skipped the short `80:2` phrase and preserved ordered anchors from `80:1` plus `80:3`; strict anchor similarity also missed canonical `وتولى` vs ASR `توليه` because the leading `و` clitic was absent.
- Added selected-Surah-only bounded multi-ayah sequence-anchor locking. It requires ordered anchors across at least two ayahs, and anchor similarity now tolerates dropped leading `و` / `ف` clitics without lowering the main threshold.
- New Swift regression from the exact 18:48 log transcript first failed as `locating/coreml_local_no_match`; after the fix it locks to displayed `80:1` with `reason=coreml_local_sequence_anchor_lock` and `nextExpectedRef == 80:4:1`.
- Verification passed: focused regression, `swift test --filter CoreMLFastConformerTests` with 25 Swift Testing tests, full Swift client core with 33 XCTest plus 56 Swift Testing tests, macOS `TarteelPrototypeMac` build, and iOS simulator `TarteelPrototypeCoreMLReplay` build.
- Remaining risk: fresh manual mic retest is still needed; if ASR drops too many anchors, locator broadening will hit diminishing returns and preprocessing/decoder quality should become the next target.
- Latest slice: selected-Surah CoreML Surah 80 post-lock rolling forward progression, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- After the first-lock fix, post-lock progression still lagged because cumulative CoreML ASR kept stale `80:2` text and mutated the last previous word, so exact word-overlap diffing failed and the current/next matcher could re-progress to `80:2` instead of following the later recitation.
- Prefix locks now preserve the displayed start ayah while carrying an internal `nextExpectedRefOverride`; the Surah 80 basmala/ayah 1/ayah 2 lock displays `80:1` but internally expects `80:3:1`.
- Added a selected-Surah-only rolling suffix forward-progress fallback after normal ordered current/next matching. It searches a bounded forward window in the selected surah using recent suffix word windows, then emits `coreml_local_ordered_forward_progress` for clear later evidence.
- New Swift regression proves the noisy Surah 80 cumulative transcript ending in `...جاءك يسعى` now progresses to `80:8` with `nextExpectedRef == 80:9:1` instead of lagging on stale `80:2`.
- Verification passed: red regression failed first with stale `80:2`; after the fix the focused regression passed, `swift test --filter CoreMLFastConformerTests` passed with 24 Swift Testing tests, full Swift client core passed with 33 XCTest tests plus 55 Swift Testing tests, focused Apple/CoreML Python guardrails passed with 25 tests, full Python passed with 297 tests, compileall passed, structured JSON/active-feature/diff/debug-grep checks passed, and both macOS `TarteelPrototypeMac` plus iOS simulator `TarteelPrototypeCoreMLReplay` builds succeeded.
- Remaining risk: this can advance several short ayahs at once if ASR only becomes clear later. It is bounded and selected-Surah-only, but manual macOS testing still needs to confirm the visible sequence feels right.
- Latest slice: selected-Surah Surah 80 prefix-span first-lock diagnosis/fix, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Corrected diagnosis: Tanzil is not missing basmala. The root `data/tanzil/quran-simple-clean.txt` and the built macOS app bundle both contain `80|1|بسم الله الرحمن الرحيم عبس وتولى`.
- The Surah 80 manual failure was caused by single-ayah pre-lock scoring against cumulative ASR. The noisy transcript at chunk 62 covered basmala/`80:1` into `80:2`, but scored about `0.52` against long `80:1` and about `0.83` against short `80:2`, so the old selected-Surah matcher locked at `80:2` with `start_ref=80:2:1`.
- Added a selected-Surah-only prefix-span lock that can override a later pre-lock candidate when bounded multi-ayah ordered LCS coverage shows the transcript began earlier in the selected surah.
- New Swift coverage proves the exact Surah 80 log transcript now locks at `80:1` with `reason=coreml_local_prefix_lock`, while clean later recitation at `80:8` still locks to `80:8` and is not dragged backward.
- Verification passed: red Surah 80 test failed first with `80:2`; after the fix the focused regression passed, `swift test --filter CoreMLFastConformerTests` passed with 23 Swift Testing tests, and full Swift client core passed with 33 XCTest tests plus 54 Swift Testing tests.
- Follow-up completed in the latest slice: span-aware expected refs and rolling suffix progression now handle the next observed post-lock lag pattern.
- Latest slice: selected-Surah CoreML anchor lock for noisy long-ayah live transcripts, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- The failed macOS Surah 35 run was caused by first-lock strictness, not by CoreML wiring: logs showed `scope=35`, bundled full Tanzil `ayahs=6236`, correct 2,560-sample app chunks over 17,920-sample model windows, and repeated `coreml_local_no_match` despite cumulative transcript anchors from 35:1.
- `CoreMLLocalQuranSession` now has a selected-Surah-only ordered anchor fallback for initial locks. It runs only after exact/span/tolerant matching fails, stays disabled for auto-detect, requires at least five ordered content-word anchors and 30% anchor coverage, and emits `coreml_local_anchor_lock`.
- New Swift regressions cover both sides of the Surah 35 failure: the noisy preface alone stays `locating/coreml_local_no_match`, while the full noisy cumulative transcript from the latest logs locks to `35:1` and sets `nextExpectedRef` to `35:2:1`.
- Final verification for this slice passed: focused Surah 35 red/green, `swift test --filter CoreMLFastConformerTests` with 21 tests, full Swift client core with 33 XCTest tests plus 52 Swift Testing tests, focused Apple/CoreML Python guardrails with 25 tests, macOS `TarteelPrototypeMac` build, and iOS simulator `TarteelPrototypeCoreMLReplay` build after rerunning outside the sandbox with temporary module caches.
- Latest slice: selected-Surah CoreML locator and chunk cadence optimization, completed locally on 2026-06-10 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- `CoreMLLocalQuranSession` now locks inside the selected Surah and, after lock, prefers ordered progression across only the current ayah and next ayah. It tracks `start_ref`, `next_expected_ref`, `consumed_words`, and incremental cumulative-ASR words, and it avoids broad relocking after a miss.
- `RecitationSessionState` now retains canonical word state: `nextExpectedRef`, `currentAyahWords`, and `completedWordCount`. The iPhone and macOS primary recitation surfaces render Tanzil ayah words with completed-word highlighting; raw ASR transcript remains diagnostic.
- iPhone and macOS microphone streamers now coalesce hardware callbacks into 2,560-sample, 160 ms chunks at 16 kHz. The CoreML model window remains fixed at 17,920 samples, so exactly seven app chunks fill one model window.
- CoreML diagnostics now include `coreml_asr_quran_corpus` and `coreml_asr_locator_event` markers with event type, reason, ayah ref, start ref, next expected ref, consumed words, chunk sequence, and confidence.
- Red Swift tests first failed for ordered Surah 108 progress and chunk divisibility; red reducer-state tests first failed before canonical word fields existed. After implementation, focused CoreML/state Swift checks passed with 30 tests and full Swift client core passed with 33 XCTest tests plus 50 Swift Testing tests.
- Focused Apple Python guardrails passed with 22 tests; full Python passed with 297 tests; compileall and feature-list JSON validation passed.
- macOS app build passed with `TarteelPrototypeMac`. iOS simulator build passed with the existing shared `TarteelPrototypeCoreMLReplay` scheme; the older plain `TarteelPrototype` scheme is not currently shared in this project.
- Latest slice: CoreML default app fallback, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `RecitationPreferencesDefaults.coreMLSelectedSurah108`; normal iPhone and macOS app entrypoints now use `UserDefaultsRecitationPreferencesStore(fallbackValues: .coreMLSelectedSurah108)` when no replay arguments are present.
- Fresh app installs now default to CoreML + selected Surah 108 for local testing. Existing saved preferences still override the fallback; reset app preferences or manually choose CoreML if an installed app keeps an older preset.
- Red Swift/Python default guardrails failed before the fallback and app wiring existed; focused default checks then passed.
- Focused Apple/CoreML Python guardrails passed with 22 tests; focused Swift CoreML/preferences/view-model checks passed with 33 XCTest tests plus 16 Swift Testing tests, including the real `108001.wav` view-model CoreML replay locking `108:1`.
- `TarteelPrototypeCoreMLReplay` generic `iphoneos` build passed after the default change, and `TarteelPrototypeMac` macOS build passed after the default change.
- Latest slice: shared physical-device CoreML replay scheme, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `ios/TarteelPrototype/TarteelPrototype.xcodeproj/xcshareddata/xcschemes/TarteelPrototypeCoreMLReplay.xcscheme`; it builds and launches the iPhone `TarteelPrototype.app` target with `--tarteel-replay-audio 108001.wav --tarteel-replay-surah 108`.
- Red Python guardrail first failed because the scheme was missing, then `uv run python -B -m unittest tests.test_macos_app_project.MacOSAppProjectTests.test_project_has_shared_coreml_replay_scheme_for_physical_device_testing -v` passed.
- Xcode now lists the shared scheme: `xcodebuild -list -project ios/TarteelPrototype/TarteelPrototype.xcodeproj` listed `TarteelPrototypeCoreMLReplay` and `TarteelPrototypeMac`.
- Generic physical-device build passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeCoreMLReplay -sdk iphoneos -destination generic/platform=iOS -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO -disableAutomaticPackageResolution build`; output ended with `** BUILD SUCCEEDED **` and copied six local audio replay fixtures into the `Debug-iphoneos` app bundle.
- Focused Apple/CoreML guardrails passed with 21 tests: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_coreml_fixture_runner -v`.
- `xcrun xctrace list devices` shows no connected physical iPhone, only the Mac under Devices and iPhones under Simulators; real iOS CoreML ASR acceptance still requires connecting a physical Apple Neural Engine device and running this scheme.
- Latest slice: actionable CoreML invalid-output UI and replay recheck, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- `RecitationViewModel` now maps `CoreMLFastConformerError.invalidModelOutput` under the `CoreML` preset to a user-facing message that explicitly says to test iOS on a physical Apple Neural Engine device rather than Simulator.
- The iPhone `ContentView` runtime error text now wraps vertically; XcodeBuildMCP screenshot `/var/folders/b1/_3ms4wf1765fjddl0nb083580000gn/T/screenshot_optimized_33821a39-cb8c-41fc-a02f-c362421485df.jpg` shows the bottom error message fully readable on the iPhone 17 Pro Simulator.
- Fresh iOS Simulator replay with `--tarteel-replay-audio 108001.wav --tarteel-replay-surah 108` produced the expected invalid-output evidence in OSLog `/Users/mostafa/Library/Developer/XcodeBuildMCP/workspaces/tarteel-realtime-a36863963df4/logs/dev.mostafa.TarteelPrototype_oslog_2026-06-09T13-57-39-451Z_helperpid27333_ownerpid44328_3a5c356a.log`: `coreml_asr_model_loaded`, buffering, `coreml_asr_invalid_output reason=nonfinite_logprobs detail=timestep=0 nonfinite_values=1025`, disconnect, and reset.
- Fresh macOS/CoreML runner proof still works: `swift run coreml-fixture-runner --model-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.models/fastconformer-quran-coreml-streaming --audio /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio/108001.wav --manifest /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.worktrees/coreml-fastconformer-spike/fixtures/local_audio_manifest.json` produced final transcript `أَعْطَيْنَاكَ الْكَوْثَرَ` and normalized word score `0.667`.
- Latest focused checks passed: Swift CoreML/preferences/view-model filters with 32 XCTest tests plus 16 Swift Testing tests, and `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_coreml_fixture_runner -v` with 20 tests.
- Latest broad checks passed: full Swift client core with 32 XCTest tests plus 46 Swift Testing tests, full deterministic Python suite with 293 tests, `uv run python -m compileall -q tarteel_realtime tests`, and macOS app xcodebuild.
- Previous slice: iOS Simulator CoreML replay diagnosis and invalid-output guard, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- iOS Simulator replay with `--tarteel-replay-audio 108001.wav --tarteel-replay-surah 108` now launches through XcodeBuildMCP, loads the bundled FastConformer model, buffers real replay chunks, and then fails loudly with `coreml_asr_invalid_output reason=nonfinite_logprobs detail=timestep=0 nonfinite_values=1025` instead of silently emitting blank transcript windows.
- Temporary instrumentation showed the iOS Simulator path receives non-silent samples and nonzero log-mel features, but the raw Float16 `logprobs` buffer from the Simulator-compiled model starts with `FE00` and is nonfinite. The model metadata/README describe this streaming model as fixed-shape fp16 and ANE-specialized, so physical Apple Neural Engine hardware is the required iOS acceptance environment.
- `CoreMLFastConformerTranscriber` now reports unexpected/nonfinite model tensors through `coreml_asr_invalid_output` and throws `CoreMLFastConformerError.invalidModelOutput`; the failed `.cpuOnly` Simulator experiment and `[DEBUG-iosblank]` probes were removed.
- macOS fixture replay still works after the guard: `swift run coreml-fixture-runner --model-dir ../../.models/fastconformer-quran-coreml-streaming --audio /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio/108001.wav` produced final transcript `أَعْطَيْنَاكَ الْكَوْثَرَ`.
- Previous slice: CoreML FastConformer app-owned local audio replay, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `VolatileRecitationPreferencesStore` so developer replay can force `.coreML` + `.selectedSurah` without writing those choices to normal app preferences.
- `TarteelPrototypeApp` and `TarteelPrototypeMacApp` now parse `--tarteel-replay-audio <wav>` and optional `--tarteel-replay-surah <id>`, resolve the audio from absolute paths or bundled `local_audio/`, inject `LocalAudioReplayStreamer`, auto-start `RecitationViewModel.startRecording()`, and replay chunks through the same queued audio, real VAD metadata, CoreML socket, and local locator path.
- Added `ios/TarteelPrototype/Scripts/copy-local-audio-fixtures.sh` and `Copy Local Audio Fixtures` phases for both app targets. When ignored local root WAV fixtures exist under `/Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio`, both app builds copy them into bundled `local_audio/`; fresh checkouts without those fixtures remove stale copies and continue to build.
- Bundle inspection found all six WAV fixtures in both built apps: `004001.wav`, `004002.wav`, `004003.wav`, `108001.wav`, `108002.wav`, and `108003.wav`. Root, iOS-bundled, and macOS-bundled `108001.wav` all matched SHA-256 `86b655403b3da9e2baed9b0f2ba230b0dafe4ee7ec9c4b67cc0eee1ae36a4789`.
- Launched the built macOS app with `open -n /private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app --args --tarteel-replay-audio 108001.wav --tarteel-replay-surah 108`; unified logs from `TarteelPrototypeMac` showed `coreml_asr_connect url=coreml://fastconformer-quran-streaming?scope=108`, model load, real CoreML transcript windows, and cumulative transcript ending at `أَعْطَيْنَاكَ الْكَوْثَرَ`.
- Previous slice: CoreML FastConformer app-build Tanzil resource bundling, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `ios/TarteelPrototype/Scripts/copy-local-tanzil-resource.sh` and `Copy Local Tanzil Quran` shell-script phases for both `TarteelPrototype` and `TarteelPrototypeMac`.
- When the ignored local root file `/Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/data/tanzil/quran-simple-clean.txt` exists, both app builds copy it into their app bundle resources as `quran-simple-clean.txt`; fresh checkouts without the file keep the CoreML local session's MVP fallback behavior.
- Bundle inspection found `quran-simple-clean.txt` in both `/private/tmp/tarteel-xcode-derived/Build/Products/Debug-iphonesimulator/TarteelPrototype.app/` and `/private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app/Contents/Resources/`, both 794,313 bytes with SHA-256 `054b3d9f79c0c2e44df7f9ddf42561797b3b5cb4fbdafbf2e99c805ccf1a6b49`.
- Previous slice: CoreML FastConformer Tanzil-format local corpus loading, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `CoreMLLocalQuranCorpus.ayahs(fromTanzilText:)` / `ayahs(fromTanzilURL:)` and `CoreMLLocalQuranCorpusError` so the Swift CoreML local locator can parse Tanzil `surah|ayah|text` data without committing the ignored full Quran text into source.
- `CoreMLFastConformerEngine` now builds its local Quran session from `quran-simple-clean.txt` when that file is bundled in the app or present beside a local model directory; if absent, it preserves the existing MVP Surah 108 / local Surah 4 fallback corpus.
- New Swift coverage proves a Tanzil-backed corpus can lock Surah 114:2 beyond the hard-coded MVP ayahs, malformed rows fail loudly, and the local pinned full file parses to 6,236 ayahs when present.
- Previous slice: CoreML FastConformer app-level fixture replay, completed locally on 2026-06-09 in `.worktrees/coreml-fastconformer-spike` on branch `codex/coreml-fastconformer-spike`.
- Added `CoreMLFastConformerSocketClient(modelDirectoryURL:)` so Swift tests/tools can load the local gated model directory without changing the iOS/macOS app bundle path.
- Added an opt-in `RecitationViewModel` replay test for `108001.wav`: fixture audio is emitted as live-sized 16 kHz chunks, every queued chunk passes through the VAD metadata seam, the real CoreML socket runs inference, and shared app state locks on `108:1` with canonical ayah text.
- Previous slice: CoreML FastConformer app-side locator MVP, completed locally on 2026-06-08 in `.worktrees/coreml-fastconformer-spike`.
- The CoreML route now preserves selected Surah scope in `coreml://fastconformer-quran-streaming?scope=<surah-id>` and maps cumulative CoreML transcripts through a minimal local Swift Quran session before sending events to the shared app reducer.
- Added a tiny MVP local corpus for Surah 108 plus the local Surah 4 fixture ayahs. It emits compatible `locked` / `progress` events with canonical `ayah_ref`, `ayah_text`, `start_ref`, `next_expected_ref`, and `consumed_words`.
- The reducer can now update visible ayah state from progress events that carry ayah metadata, and CoreML-specific buffering events use the existing gathering-audio state.
- Real fixture smoke after this slice still produced `108001.wav -> أَعْطَيْنَاكَ الْكَوْثَرَ`, normalized word score `0.667`, average inference `25.5ms/window`.
- Previous slice: CoreML FastConformer local WAV fixture scoring gate, completed locally on 2026-06-08.
- Added committed benchmark metadata at `fixtures/local_audio_manifest.json`; the audio remains ignored/local under `/Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio`.
- `coreml-fixture-runner` now accepts `--manifest <path>` in addition to `--model-dir`, `--audio-dir`, repeated `--audio`, `--json`, and `--no-pad-final-window`.
- Added `CoreMLFastConformerFixtureManifest`, `CoreMLFastConformerFixtureExpectation`, and `CoreMLFastConformerFixtureScore`; scored reports now show normalized expected/actual text, word score, WER, CER, missing words, extra words, substitutions, and average inference latency.
- Scored fixture baseline: `108001=0.667`, `108002=0.333`, `108003=1.000`, `004001=0.517`, `004002=0.312`, `004003=0.214` normalized word score. Short Surah 108 is still the promising path; long Surah 4 remains too noisy for correction UX.
- Previous slice: CoreML FastConformer local WAV fixture harness, completed locally on 2026-06-08.
- Added SwiftPM executable `coreml-fixture-runner` under `ios/TarteelClientCore`; it loads the local gated model artifacts from `/Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.models/fastconformer-quran-coreml-streaming` and replays local WAV fixtures from `/Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio`.
- Added `CoreMLFastConformerFixtureAudio` for PCM16 WAV parsing, stereo downmix, and whole-file 16 kHz resampling; added structured fixture window/report output with per-window transcript, cumulative transcript, confidence, token count, and inference timing.
- Previous slice: CoreML FastConformer streaming transcript stitching for the Apple prototypes, completed locally on 2026-06-07.
- The manual macOS recitation proved CoreML inference was running and produced Al-Ikhlas-like fragments, but the log output was per-window only.
- The model README documents fixed non-overlapping `112` mel-frame chunks with cache carryover; do not add raw audio overlap without stronger model-level evidence.
- `CoreMLFastConformerCTCDecoder.decode` now supports `trimsWhitespace: false`, and the transcriber preserves SentencePiece word-boundary spaces after the first cumulative transcript text.
- `coreml_asr_transcript` now includes `cumulative_transcript` beside the per-window `transcript` so manual recitation quality can be checked directly from unified logs.
- Previous slice: CoreML FastConformer ASR diagnostics for the Apple prototypes, completed locally on 2026-06-07.
- Added `CoreMLFastConformerDiagnostics` with unified-log markers `coreml_asr_connect`, `coreml_asr_model_loaded`, `coreml_asr_reset`, `coreml_asr_buffering`, `coreml_asr_blank`, `coreml_asr_transcript`, and `coreml_asr_disconnect`.
- Each prediction window now logs `chunk_sequence`, `window_index`, `inference_ms`, `confidence`, emitted token count, transcript text when present, and cumulative transcript text when present.
- Buffering logs are throttled to chunk 0 and every 10 chunks; transcript logs are public in this local developer prototype so the agent can inspect ASR output after manual tests.
- Previous slice: local CoreML FastConformer ASR spike for the Apple prototypes, completed locally on 2026-06-07.
- The Hugging Face model repo is gated-public; the initial fine-grained token attempt fetched only the README/cache metadata until public gated repository access was enabled for the token.
- The model artifacts now live locally under `.models/fastconformer-quran-coreml-streaming`, and the CoreML spike worktree uses a `.models` symlink to that ignored root model directory.
- Added `BackendEndpointPreset.coreML` with URL `coreml://fastconformer-quran-streaming`; it is selectable like other presets but does not expose manual URL editing or require WebSocket URL validation.
- Added `RoutingBackendSocketClient`, which preserves normal `BackendWebSocketClient` behavior for `ws://` and `wss://` URLs while routing `coreml://` URLs to `CoreMLFastConformerSocketClient`.
- Added the local CoreML FastConformer client/engine in `ios/TarteelClientCore`: it loads the bundled model and `tokens.txt`, buffers/resamples PCM to 16 kHz, computes 80x112 log-mel windows, carries streaming caches, runs CoreML prediction, CTC-decodes transcripts, and now maps matched local transcripts into compatible Quran `locked` / `progress` events for the MVP local corpus.
- The iPhone and macOS app initializers both inject `RoutingBackendSocketClient(coreML: CoreMLFastConformerSocketClient(bundle: .main))`.
- The Xcode project bundles `fastconformer-quran-streaming.mlpackage`, `pronunciation-head.mlpackage`, `tokenizer.model`, and `tokens.txt` into both `TarteelPrototype` and `TarteelPrototypeMac`; Xcode compiles the model packages into `.mlmodelc`.
- Previous slice: Quran logo app-icon fix for the Apple prototypes, completed locally on 2026-06-07.
- Root cause: the previous logo slice bundled and rendered `quran_logo.png` inside the app, but the project still had no `Assets.xcassets/AppIcon.appiconset` or `ASSETCATALOG_COMPILER_APPICON_NAME`, so the iOS simulator home screen used the default placeholder icon.
- Added `ios/TarteelPrototype/TarteelPrototype/Assets.xcassets/AppIcon.appiconset` with opaque iPhone, iOS marketing, and macOS icon renditions generated from the provided logo.
- Added `Assets.xcassets` to both `TarteelPrototype` and `TarteelPrototypeMac` resource phases and set `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon` for both targets.
- Uninstalled the stale iOS simulator app before rebuilding, so SpringBoard refreshed the visible home-screen icon.
- Fresh simulator screenshot `/private/tmp/tarteel-appicon-home.png` shows the Quran logo as the `TarteelPrototype` home-screen icon.
- macOS build now emits `Contents/Resources/AppIcon.icns` and the built macOS `Info.plist` declares `CFBundleIconName = AppIcon`.
- Previous slice: Quran logo branding for the Apple prototype in-app surfaces, completed locally on 2026-06-07.
- Copied the provided 512x512 RGBA `quran_logo.png` into `ios/TarteelPrototype/TarteelPrototype/Resources/quran_logo.png`.
- Added that PNG to the existing hand-authored Xcode project resource pattern so both `TarteelPrototype` and `TarteelPrototypeMac` copy it into their app bundles.
- Added `QuranLogoMark` SwiftUI views using `Image("quran_logo")` on the iPhone home screen and macOS recitation header.
- Added focused source/resource guardrails requiring the logo resource in both Apple app targets and visible logo usage in both SwiftUI surfaces.
- The original root-level `quran_logo.png` provided by the user remains in place; the built apps use the copied resource under `ios/TarteelPrototype/TarteelPrototype/Resources/`.
- Previous slice: post-lock rolling ASR overlap progression fix, completed locally on 2026-06-07 in `.worktrees/scoped-tolerant-lock-progression` on branch `codex/scoped-tolerant-lock-progression`.
- Fixed the next scoped Surah 4 diagnostic finding after tolerant initial lock: after the branch advances to `next_expected_ref=4:1:10`, seq 9 transcript `رَبَّكُمُ الَّذِي` now progresses instead of reporting `wrong/extra_word`.
- Added deterministic session-transition regression `test_post_lock_progress_ignores_leading_recently_consumed_word`.
- Implementation is narrow and ordered/progression-local: post-lock alignment retries only after a zero-consumed `extra_word` failure, only when the first recognized word equals the immediate previous canonical same-ayah word, and only accepts the retry when the remaining transcript correctly consumes at least one expected word.
- The fixed seq 9 event is `progress` with `consumed_words=1` and `next_expected_ref=4:1:11`.
- Previous slice: scoped tolerant initial-lock progression fix, completed locally on 2026-06-07 in `.worktrees/scoped-tolerant-lock-progression` on branch `codex/scoped-tolerant-lock-progression`.
- Fixed the Surah 4 scoped diagnostic finding where a confirmed `tolerant_match` locked at `4:1:7` with `consumed_words=0` and kept `next_expected_ref=4:1:7`.
- Added a deterministic session-transition regression for seq 5 `يَا وَالنَّاسُتَّ`, seq 6 waiting, and seq 7 `وَالنَّاسُتَّ اتَّقُوا رَبَّكُ`.
- The fixed locked event still starts at `4:1:7`, reports `reason=tolerant_match`, now consumes the locator candidate span (`consumed_words=3`), and advances to `next_expected_ref=4:1:10`.
- Implementation is narrow: confirmed initial `tolerant_match` with zero aligner-consumed words advances from the locator candidate span; locator scoring, scope filtering, and tolerant confirmation policy are unchanged.
- Existing unscoped noisy/tolerant safety behavior remains covered by the focused session tests.
- Previous slice: replay-based visual diagnostics bundle generation for realtime recitation performance analysis, completed locally on 2026-06-07.
- Design spec: `docs/superpowers/specs/2026-06-06-visual-diagnostics-tool.md`.
- Implementation plan: `docs/superpowers/plans/2026-06-06-visual-diagnostics-tool.md`.
- V1 runtime scope: opt-in `/ws/recitation?diagnostics=1` trace envelopes plus `tarteel_realtime.diagnostics_capture`, which replays a WAV and writes an ignored static HTML bundle under `diagnostics/sessions/`.
- Normal `/ws/recitation` remains unchanged when diagnostics are not requested.
- Diagnostic bundles include raw mic audio, ASR input/window WAV artifacts, VAD metadata when supplied by the client, buffering decisions, ASR timing/transcripts, locator/alignment decisions, and sanitized metadata.
- Local dev-backend capture smoke with a temporary mono PCM16 WAV wrote `diagnostics/sessions/20260607T040113Z-tarteel-diagnostics-smoke-scope-auto/index.html`.
- macOS live recording/export remains a follow-up; the implemented v1 answers what reaches ASR exactly through replay capture.
- Latest slice: macOS live-recitation timeline UX, completed locally on 2026-06-03.
- The macOS right panel now shows a curated `Timeline` instead of raw `Recent Events` copy.
- `RecitationViewModel` collapses adjacent repeated event signatures into one history row with `repeatCount` / `repeatBadgeText`; repeated rows keep the latest transcript and chunk sequence.
- Repeated `uncertain` / `waiting_for_audio_buffer` and repeated same-ayah `progress` events no longer flood the visible timeline; new-ayah progress remains separate.
- Previous slice: Modal macOS default and Keychain token persistence, completed locally on 2026-06-03.
- Modal is the macOS first-launch `Custom` provider default, including the deployed WSS URL `wss://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run/ws/recitation`.
- macOS injects `UserDefaultsRecitationPreferencesStore(fallbackValues: .modalPrimary)`, and persisted preferences override the fallback.
- macOS stores the selected `Custom` provider bearer token in Keychain through `KeychainBackendBearerTokenStore`; iPhone remains memory-only.
- Shared `RecitationViewModel` now persists, clears, reloads, and reports selected-provider bearer token storage through an injected `BackendBearerTokenStoring` boundary.
- macOS Settings copy/tooltips now describe selected-provider Keychain persistence and surface Keychain failure messages without contradicting success copy.
- Previous slice: macOS native UI polish, completed locally on 2026-06-03.
- The macOS prototype now uses a unified compact toolbar for recording, Surah search, and Settings, with source/build-verified native shell behavior.
- Keyboard commands are wired for `Space`/`Command-R` recording and `Command-F` Surah search focus.
- The macOS recitation surface now includes macOS 14-compatible Surah filtering/selection, first-run onboarding, event history, empty states, URL/text drop-in for backend setup with visible feedback, diagnostic drag-out text, adaptive system colors/materials, and Settings validation feedback.
- Shared `RecitationViewModel` presentation state now exposes recent event history, backend URL validation, recording action metadata, shareable diagnostic summary text, and dropped backend feedback.
- Previous slice: Modal CUDA image fix, deployed on 2026-05-29.
- Modal logs for deployed app `ap-y0XxuwnT0t7dEPT8FaWe2W` / `tarteel-realtime-asr` showed repeated recitation failures:
  - `RuntimeError: Library libcublas.so.12 is not found or cannot be loaded`
  - The traceback originates in faster-whisper/CTranslate2 during `self.model.encode(...)`.
- `deploy/modal_asr_app.py` now uses `nvidia/cuda:12.4.1-cudnn-runtime-ubuntu22.04` through `modal.Image.from_registry(..., add_python="3.13")`.
  - This replaces the old `debian_slim` base image, which did not include CUDA user-space libraries.
  - The app was redeployed and Modal reported URL `https://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run`.
- Previous slice: Modal replay fixture fix, completed locally on 2026-05-29.
- `tarteel_realtime.replay_probe` now loads PCM16 WAV evidence fixtures through `load_replay_audio_file`.
  - Multi-channel PCM16 WAV files are downmixed to mono for WebSocket replay.
  - `asr_smoke` remains strict and still rejects stereo WAV input.
  - This fixes the documented `fixtures/local_audio/108001.wav` replay command, because that fixture is stereo 44.1 kHz PCM16.
- Previous slice: Modal prewarm failure fix, completed locally on 2026-05-29.
- `deploy/modal_asr_app.py` no longer uses `.uv_sync()`.
  - The image installs backend runtime dependencies explicitly through `.uv_pip_install(*PROJECT_RUNTIME_DEPENDENCIES, *MODAL_ASR_DEPENDENCIES)`.
  - This avoids Modal legacy image builder failures that require `modal` in `pyproject.toml`.
  - `modal` and `faster-whisper` remain out of default project dependencies.
- Previous slice: Modal GPU serverless provider comparison, completed locally on 2026-05-28.
- WebSocket `/ws/recitation` remains the only recitation transport.
- `deploy/modal_asr_app.py` is the Modal deployment adapter:
  - returns the existing `tarteel_realtime.asr_app:create_app_from_env` FastAPI app
  - uses `gpu="L4"`, `min_containers=0`, `max_containers=1`, `scaledown_window=60`
  - uses Modal Volume `tarteel-asr-model-cache` at `/models/huggingface-cache/hub`
  - exposes local entrypoint `prewarm` for model cache hydration
- `TARTEEL_WS_BEARER_TOKEN` enables provider-neutral WebSocket bearer auth.
  - `/health` and `/ping` remain public.
  - `WS /ws/recitation` rejects missing or wrong bearer tokens when configured.
- `tarteel_realtime.replay_probe` is the provider-neutral RunPod/Modal evidence tool.
  - It supports `--scope`, `--bearer-token`, `--disable-ping`, and `--include-events`.
  - It reports connect time, total time, first non-wait event timing, event counts, and first lock/progress refs.
- Apple settings now keep `Simulator` and `Custom`.
  - Under `Custom`, provider picker options are `Generic`, `RunPod`, and `Modal`.
  - RunPod normalizes bare `.proxy.runpod.net` and `.api.runpod.ai` hosts.
  - Modal normalizes bare `.modal.run` hosts.
  - iPhone bearer tokens remain memory-only.

## Verification

- Selected-Surah CoreML anchor-lock checks passed:
  - Red Surah 35 Swift test first failed with `type=locating`, `reason=coreml_local_no_match`, and no ayah ref for the latest noisy macOS cumulative transcript.
  - After implementation, the noisy preface-only transcript remained unlocked, and the full noisy Surah 35:1 transcript locked with `reason=coreml_local_anchor_lock`, `ayah_ref=35:1`, `start_ref=35:1:1`, and `next_expected_ref=35:2:1`.
  - `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests` passed with 21 tests.
  - Full Swift client core passed with 33 XCTest tests plus 52 Swift Testing tests.
  - Focused Apple/CoreML Python guardrails passed with 25 tests: `uv run python -B -m unittest tests.test_coreml_fixture_runner tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_ios_audio_streamer -v`.
  - macOS app build passed for scheme `TarteelPrototypeMac`.
  - iOS simulator app build passed for shared scheme `TarteelPrototypeCoreMLReplay`; the first sandboxed run failed on CoreSimulator/cache permissions and the successful run used the approved outside-sandbox temporary module-cache command.
- Selected-Surah CoreML locator/chunk cadence checks passed:
  - Red/green Swift CoreML tests covered Surah 108 ordered progress from ayah 1 into ayah 2, noisy scoped Surah 99 lock, no broad post-lock jumping, and 17,920 / 2,560 chunk cadence.
  - Red/green Swift reducer tests covered canonical ayah words and highlighted progress derived from `nextExpectedRef`.
  - `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests --filter RecitationSessionStateTests` passed with 30 tests.
  - Full Swift client core passed with 33 XCTest tests plus 50 Swift Testing tests.
  - `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_ios_audio_streamer -v` passed with 22 tests.
  - Full deterministic Python passed with 297 tests; `uv run python -m compileall -q tarteel_realtime tests` passed; `uv run python -B -m json.tool feature_list.json` passed.
  - macOS build passed for scheme `TarteelPrototypeMac`.
  - iOS simulator build passed for shared scheme `TarteelPrototypeCoreMLReplay` after rerunning outside the sandbox because CoreSimulatorService and user cache access were blocked inside the sandbox.
- CoreML actionable invalid-output UI checks passed:
  - Red Swift view-model test first failed on generic CoreML error copy; after the mapping change, `testCoreMLInvalidModelOutputShowsActionableError` passed.
  - Red Python source guardrail first failed on missing wrapped runtime error modifiers; after the `ContentView` update, `test_content_view_allows_runtime_errors_to_wrap` passed.
  - XcodeBuildMCP `build_run_sim` with `--tarteel-replay-audio 108001.wav --tarteel-replay-surah 108` succeeded and captured OSLog `/Users/mostafa/Library/Developer/XcodeBuildMCP/workspaces/tarteel-realtime-a36863963df4/logs/dev.mostafa.TarteelPrototype_oslog_2026-06-09T13-57-39-451Z_helperpid27333_ownerpid44328_3a5c356a.log`; the log contains `coreml_asr_invalid_output reason=nonfinite_logprobs detail=timestep=0 nonfinite_values=1025`.
  - Screenshot `/var/folders/b1/_3ms4wf1765fjddl0nb083580000gn/T/screenshot_optimized_33821a39-cb8c-41fc-a02f-c362421485df.jpg` shows the user-visible bottom error fully readable.
  - Focused Swift checks passed with 32 XCTest tests plus 16 Swift Testing tests; focused Python Apple/CoreML guardrails passed with 20 tests.
  - Fresh `108001.wav` CoreML runner replay passed with final transcript `أَعْطَيْنَاكَ الْكَوْثَرَ` and normalized word score `0.667`.
  - Full Swift client core passed with 32 XCTest tests plus 46 Swift Testing tests; full Python passed with 293 tests; compileall passed; macOS xcodebuild passed.
- CoreML iOS Simulator replay diagnosis / invalid-output guard checks passed:
  - Red project guardrail first failed because `coreml_asr_invalid_output`, `nonfinite_logprobs`, and `isFinite` handling were absent from `CoreMLFastConformerClient.swift`.
  - Focused guardrail passed: `uv run python -B -m unittest tests.test_macos_app_project.MacOSAppProjectTests.test_coreml_client_reports_nonfinite_model_output -v`.
  - macOS SwiftPM replay still passed after the guard: `swift run coreml-fixture-runner --model-dir ../../.models/fastconformer-quran-coreml-streaming --audio /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio/108001.wav`, final transcript `أَعْطَيْنَاكَ الْكَوْثَرَ`.
  - iOS Simulator app replay launched with XcodeBuildMCP `build_run_sim` and launch args `--tarteel-replay-audio 108001.wav --tarteel-replay-surah 108`; OSLog path `/Users/mostafa/Library/Developer/XcodeBuildMCP/workspaces/tarteel-realtime-a36863963df4/logs/dev.mostafa.TarteelPrototype_oslog_2026-06-09T13-13-39-567Z_helperpid4252_ownerpid44328_a6e07c82.log` showed `coreml_asr_model_loaded`, buffering, and `coreml_asr_invalid_output reason=nonfinite_logprobs detail=timestep=0 nonfinite_values=1025`.
  - Focused Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_coreml_fixture_runner -v` with 19 tests.
  - Focused Swift checks passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests --filter RecitationViewModelTests --filter RecitationPreferencesStoreTests` with 31 XCTest tests plus 16 Swift Testing tests.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - Full Swift client core passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 31 XCTest tests plus 46 Swift Testing tests.
  - Full deterministic Python suite passed: `uv run python -B -m unittest discover -s tests -v` with 292 tests.
  - Compile check passed: `uv run python -m compileall -q tarteel_realtime tests`.
  - Harness checks passed: `uv run python -B -m json.tool feature_list.json`, active-feature sanity returned `[]`, no temporary debug-probe code strings remained in `CoreMLFastConformerClient.swift` / `tests/test_macos_app_project.py`, and `git diff --check` passed.
- CoreML app-owned local audio replay checks passed:
  - Red Swift preference guardrail first failed because `VolatileRecitationPreferencesStore` did not exist.
  - Red Python app guardrail first failed because `copy-local-audio-fixtures.sh` was missing and app entrypoints did not mention `LocalAudioReplayConfiguration`.
  - Focused replay/preferences Swift checks passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter RecitationPreferencesStoreTests/testVolatileStoreKeepsDeveloperReplayPreferencesInMemory --filter CoreMLFastConformerTests/localAudioReplayStreamerEmitsLiveSized16KChunks --filter CoreMLFastConformerTests/localAudioReplayConfigurationParsesFixtureLaunchArguments`.
  - Focused app guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project.MacOSAppProjectTests.test_project_includes_shared_core_files_in_both_app_targets tests.test_macos_app_project.MacOSAppProjectTests.test_project_conditionally_bundles_local_audio_replay_fixtures tests.test_macos_app_project.MacOSAppProjectTests.test_apps_support_developer_coreml_audio_replay_launch_argument -v`.
  - Focused Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_coreml_fixture_runner -v` with 18 tests.
  - Focused Swift CoreML/preferences/view-model checks passed with 31 XCTest tests plus 16 Swift Testing tests.
  - iOS simulator build passed via XcodeBuildMCP `build_sim` for `TarteelPrototype` on `iPhone 17 Pro`; the build log showed `Copied 6 local audio replay fixture(s)`.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`; the build log showed `Copied 6 local audio replay fixture(s)`.
  - Bundle inspection passed: iOS and macOS bundles both contain `004001.wav`, `004002.wav`, `004003.wav`, `108001.wav`, `108002.wav`, and `108003.wav` under `local_audio/`; `108001.wav` SHA-256 matched the root local fixture at `86b655403b3da9e2baed9b0f2ba230b0dafe4ee7ec9c4b67cc0eee1ae36a4789`.
  - macOS app-binary replay passed: launching `TarteelPrototypeMac.app` with `--tarteel-replay-audio 108001.wav --tarteel-replay-surah 108` produced `coreml_asr_transcript` logs ending at cumulative transcript `أَعْطَيْنَاكَ الْكَوْثَرَ`.
  - Full Swift client core passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 31 XCTest tests plus 46 Swift Testing tests.
  - Full deterministic Python suite passed: `uv run python -B -m unittest discover -s tests -v` with 291 tests.
  - Compile check passed: `uv run python -m compileall -q tarteel_realtime tests`.
  - Whitespace check passed: `git diff --check`.
- CoreML app-build local Tanzil resource bundling checks passed:
  - Red Python guardrail first failed because `ios/TarteelPrototype/Scripts/copy-local-tanzil-resource.sh` did not exist.
  - Focused project guardrail passed: `uv run python -B -m unittest tests.test_macos_app_project.MacOSAppProjectTests.test_project_conditionally_bundles_local_tanzil_quran_text -v`.
  - iOS simulator build passed via XcodeBuildMCP `build_sim` using project `/Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.worktrees/coreml-fastconformer-spike/ios/TarteelPrototype/TarteelPrototype.xcodeproj`, scheme `TarteelPrototype`, simulator `iPhone 17 Pro`, derived data `/private/tmp/tarteel-xcode-derived`, and `CODE_SIGNING_ALLOWED=NO`.
  - iOS build log showed the copy phase and `Copied local Tanzil Quran text from .../../../../../data/tanzil/quran-simple-clean.txt`.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - macOS build log showed the same copy phase and local Tanzil source path.
  - Bundle inspection passed: the iPhone simulator and macOS app bundles both contain `quran-simple-clean.txt` at 794,313 bytes with SHA-256 `054b3d9f79c0c2e44df7f9ddf42561797b3b5cb4fbdafbf2e99c805ccf1a6b49`, matching the pinned local root file.
  - Focused Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_coreml_fixture_runner -v` with 16 tests.
  - Focused CoreML/view-model checks passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests --filter RecitationViewModelTests` with 24 XCTest tests plus 14 Swift Testing tests.
  - Final Swift client core passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 30 XCTest tests plus 44 Swift Testing tests.
  - Final harness checks passed: `uv run python -B -m json.tool feature_list.json`, active-feature sanity returned `[]`, `git diff --check`, and `uv run python -m tarteel_realtime.quran_data --check-manifest` reported the pinned 6,236-ayah Tanzil file and SHA-256 `054b3d9f79c0c2e44df7f9ddf42561797b3b5cb4fbdafbf2e99c805ccf1a6b49`.
  - Full deterministic Python suite passed: `uv run python -B -m unittest discover -s tests -v` with 289 tests.
  - Compile check passed: `uv run python -m compileall -q tarteel_realtime tests`.
- CoreML Tanzil-format corpus loading checks passed:
  - Red Swift checks first failed because `CoreMLLocalQuranCorpus` and `CoreMLLocalQuranCorpusError` did not exist.
  - Focused CoreML tests passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests` with 14 Swift Testing tests, including the opt-in 6,236-ayah local full-file parse.
  - App-level CoreML replay still passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter RecitationViewModelTests/testCoreMLFixtureReplayThroughViewModelLocksSelectedSurah`.
  - Focused CoreML/view-model checks passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests --filter RecitationViewModelTests` with 24 XCTest tests plus 14 Swift Testing tests.
  - Full Swift client core passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 30 XCTest tests plus 44 Swift Testing tests.
  - Focused Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_coreml_fixture_runner tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 15 tests.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
- CoreML app-level fixture replay checks passed:
  - Red Swift replay test first failed because `CoreMLFastConformerSocketClient(modelDirectoryURL:)` did not exist and `CoreMLFastConformerFixtureAudio.resampled16KPCM16` was `fileprivate`.
  - Focused app-level replay passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter RecitationViewModelTests/testCoreMLFixtureReplayThroughViewModelLocksSelectedSurah`.
  - Focused CoreML/view-model checks passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests --filter RecitationViewModelTests` with 24 XCTest tests plus 11 Swift Testing tests.
  - Full Swift client core passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 30 XCTest tests plus 41 Swift Testing tests.
  - Focused Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_coreml_fixture_runner tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 15 tests.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
- CoreML app-side locator MVP checks passed:
  - Red Swift tracer failed as expected: `swift test --filter CoreMLFastConformerTests` failed because `CoreMLLocalQuranSession` did not exist.
  - Focused CoreML/preset/state/view-model checks passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests --filter BackendEndpointPresetTests --filter RecitationSessionStateTests --filter RecitationViewModelTests` with 23 XCTest tests plus 34 Swift Testing tests.
  - Full Swift client core passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 29 XCTest tests plus 41 Swift Testing tests.
  - Focused Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_coreml_fixture_runner tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 15 tests.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
  - Real single-fixture CoreML replay passed after an escalated rerun because sandboxed SwiftPM manifest compilation was blocked: `swift run coreml-fixture-runner --model-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.models/fastconformer-quran-coreml-streaming --audio /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio/108001.wav --manifest /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.worktrees/coreml-fastconformer-spike/fixtures/local_audio_manifest.json`, final transcript `أَعْطَيْنَاكَ الْكَوْثَرَ`, normalized word score `0.667`.
  - Harness docs checks passed: `uv run python -B -m json.tool feature_list.json`, active-feature sanity returned `[]`, and `git diff --check` passed.
  - Full deterministic Python suite passed: `uv run python -B -m unittest discover -s tests -v` with 288 tests.
  - Compile check passed: `uv run python -m compileall -q tarteel_realtime tests`.
- CoreML scored fixture gate checks passed:
  - Red Swift check failed as expected because `CoreMLFastConformerFixtureScore`, `CoreMLFastConformerFixtureExpectation`, `CoreMLFastConformerFixtureManifest`, and `CoreMLFastConformerFixtureReport.scored(with:)` did not exist.
  - Red Python guardrail failed as expected because `coreml-fixture-runner` had no `--manifest` handling and `fixtures/local_audio_manifest.json` did not exist.
  - Focused Swift CoreML tests passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests` with 10 tests.
  - Focused Python CLI/manifest guardrail passed: `uv run python -B -m unittest tests.test_coreml_fixture_runner -v` with 3 tests.
  - Manifest JSON validation passed: `uv run python -B -m json.tool fixtures/local_audio_manifest.json`.
  - Full scored fixture directory replay passed: `swift run coreml-fixture-runner --model-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.models/fastconformer-quran-coreml-streaming --audio-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio --manifest /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.worktrees/coreml-fastconformer-spike/fixtures/local_audio_manifest.json`.
  - Single scored `004002.wav` replay passed to capture its untruncated score: `0.312`.
  - iOS and macOS app builds initially failed because the hand-authored Xcode project included `CoreMLFastConformerClient.swift` but not its new `CoreMLFastConformerFixtureScoring.swift` dependency.
  - Added a Python project guardrail requiring `CoreMLFastConformerFixtureScoring.swift` in both app targets; the guardrail failed red, then passed after updating `project.pbxproj`.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
- CoreML local WAV fixture harness checks passed:
  - Red Swift check failed as expected because `CoreMLFastConformerFixtureAudio`, `CoreMLFastConformerFixtureReport`, and `CoreMLFastConformerFixtureWindow` did not exist yet.
  - Red Python guardrail failed as expected because `Package.swift` had no `coreml-fixture-runner` executable and `Sources/CoreMLFixtureRunner/main.swift` did not exist.
  - Focused Swift CoreML tests passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests` with 7 tests.
  - Focused Python CLI guardrail passed: `uv run python -B -m unittest tests.test_coreml_fixture_runner -v` with 2 tests.
  - Real single-fixture replay passed: `swift run coreml-fixture-runner --model-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.models/fastconformer-quran-coreml-streaming --audio /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio/108001.wav`.
  - Full fixture directory replay passed: `swift run coreml-fixture-runner --model-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/.models/fastconformer-quran-coreml-streaming --audio-dir /Users/mostafa/Downloads/Coding_Projects/tarteel-realtime/fixtures/local_audio`.
  - Full Swift client core passed with 28 XCTest tests plus 35 Swift Testing tests.
  - Focused Python Apple/CoreML guardrails passed: `uv run python -B -m unittest tests.test_coreml_fixture_runner tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 14 tests.
  - `uv run python -B -m json.tool feature_list.json` passed.
  - `git diff --check` passed.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
- CoreML streaming transcript stitching checks passed:
  - Red Swift check failed as expected: `swift test --filter CoreMLFastConformerTests` failed because `trimsWhitespace` did not exist yet.
  - Focused Swift CoreML tests passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests` with 5 tests.
  - Full Swift client core passed with 28 XCTest tests plus 33 Swift Testing tests.
  - Focused Apple guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 12 tests.
  - `uv run python -B -m json.tool feature_list.json` passed.
  - `git diff --check` passed.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
- CoreML ASR diagnostics checks passed:
  - First manual macOS recitation log inspection before this slice showed model load, Silero VAD load, microphone capture, 48 kHz Float32 to 16 kHz Int16 conversion, and clean audio-engine stop, but no transcript or inference timing logs.
  - Focused Swift CoreML tests passed: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test --filter CoreMLFastConformerTests` with 4 tests.
  - Full Swift client core passed with 28 XCTest tests plus 32 Swift Testing tests.
  - Focused Apple guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 12 tests.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - iOS simulator app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`.
- Local CoreML FastConformer spike checks passed:
  - Red Swift check first failed as expected: `swift test --filter CoreMLFastConformerTests` failed because `BackendEndpointPreset.coreML`, `CoreMLFastConformerCTCDecoder`, and `RoutingBackendSocketClient` were missing.
  - CoreML compiler check passed: `xcrun coremlcompiler compile .models/fastconformer-quran-coreml-streaming/fastconformer-quran-streaming.mlpackage /private/tmp/tarteel-coreml-compile-check`.
  - Focused Swift CoreML tests passed: `swift test --filter CoreMLFastConformerTests` with 3 tests.
  - Endpoint preset tests passed: `swift test --filter BackendEndpointPresetTests` with 13 tests.
  - Full Swift client core passed from `ios/TarteelClientCore`: `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test` with 28 XCTest tests plus 31 Swift Testing tests.
  - Focused Apple guardrails passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 11 tests.
  - XcodeBuildMCP `build_run_sim` with `CODE_SIGNING_ALLOWED=NO` passed for scheme `TarteelPrototype` on `iPhone 17 Pro`, installed and launched `dev.mostafa.TarteelPrototype`, and captured `/var/folders/b1/_3ms4wf1765fjddl0nb083580000gn/T/screenshot_optimized_7dcbd6e4-d31c-4398-a942-e970fd279ff8.jpg`.
  - iPhone bundle inspection found `fastconformer-quran-streaming.mlmodelc`, `pronunciation-head.mlmodelc`, `tokens.txt`, and `tokenizer.model`.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - macOS bundle inspection found the same CoreML/tokenizer resources under `TarteelPrototypeMac.app/Contents/Resources`.
  - CoreML runtime smoke loaded the built macOS `fastconformer-quran-streaming.mlmodelc`, ran one zeroed prediction, and returned output shapes `[1, 13, 1025]` and `[1, 17, 70, 512]`.
  - macOS GUI launch smoke succeeded with `open -n /private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app`.
  - Broad pre-doc checks passed: `uv run python -B -m unittest discover -s tests -v` with 284 tests, `uv run python -m compileall -q tarteel_realtime tests`, and `git diff --check`.
- Quran logo app-icon checks passed:
  - Red check first failed as expected: `uv run python -B -m unittest tests.test_macos_app_project -v` failed because `Assets.xcassets` was missing from the app resource phases.
  - After the fix, `uv run python -B -m unittest tests.test_macos_app_project -v`
  - Result: 8 tests.
  - XcodeBuildMCP `build_run_sim` with `CODE_SIGNING_ALLOWED=NO` succeeded for scheme `TarteelPrototype` on `iPhone 17`, launching bundle `dev.mostafa.TarteelPrototype`.
  - iPhone bundle inspection confirmed `CFBundleIconName = AppIcon`, `AppIcon60x60@2x.png`, and opaque `AppIcon` phone renditions in `Assets.car`.
  - Fresh simulator screenshot `/private/tmp/tarteel-appicon-home.png` shows the Quran logo as the `TarteelPrototype` home-screen icon.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - macOS bundle inspection confirmed `CFBundleIconName = AppIcon`, `CFBundleIconFile = AppIcon`, and `Contents/Resources/AppIcon.icns`.
  - Final harness checks passed: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` with 11 tests, `uv run python -B -m json.tool feature_list.json`, active-feature sanity returned `[]`, `uv run python -B -m json.tool ios/TarteelPrototype/TarteelPrototype/Assets.xcassets/AppIcon.appiconset/Contents.json`, `uv run python -B -m unittest discover -s tests -v` with 283 tests, and `git diff --check`.
- Quran logo Apple app checks passed:
  - Red check first failed as expected: `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v` failed on missing `quran_logo.png` resources and missing `Image("quran_logo")` usage.
  - After the fix, `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui -v`
  - Result: 11 tests.
  - XcodeBuildMCP `build_run_sim` with `CODE_SIGNING_ALLOWED=NO` succeeded for scheme `TarteelPrototype` on `iPhone 17`, launching bundle `dev.mostafa.TarteelPrototype`.
  - macOS app build passed: `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`.
  - Bundle checks found `quran_logo.png` in `/tmp/tarteel-xcode-derived/Build/Products/Debug-iphonesimulator/TarteelPrototype.app/` and `/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app/Contents/Resources/`.
  - Final harness checks passed: `uv run python -B -m json.tool feature_list.json`, active-feature sanity returned `[]`, `uv run python -B -m unittest discover -s tests -v` passed with 283 tests, and `git diff --check` passed.
- Scoped Surah 4 post-lock overlap progression checks passed:
  - Red check first failed as expected: `uv run python -B -m unittest tests.test_session_transitions -v` failed on `test_post_lock_progress_ignores_leading_recently_consumed_word` because the event was `SessionEventType.WRONG` instead of `SessionEventType.PROGRESS`.
  - After the fix, `uv run python -B -m unittest tests.test_session_transitions -v`
  - Result: 19 tests.
  - `uv run python -B -m unittest tests.test_session tests.test_session_transitions tests.test_alignment -v`
  - Result: 44 tests.
  - `uv run python -B -m unittest discover -s tests -v`
  - Result: 284 tests.
  - `uv run python -m compileall -q tarteel_realtime tests`
- Scoped tolerant initial-lock progression checks passed:
  - Red check first failed as expected: `uv run python -B -m unittest tests.test_session_transitions -v` failed on `test_confirmed_tolerant_initial_lock_advances_by_locator_span` with `AssertionError: 0 != 3`.
  - After the fix, `uv run python -B -m unittest tests.test_session_transitions -v`
  - Result: 18 tests.
  - `uv run python -B -m unittest tests.test_session tests.test_session_transitions tests.test_alignment -v`
  - Result: 43 tests.
  - `uv run python -B -m unittest discover -s tests -v`
  - Result: 283 tests.
  - `uv run python -m compileall -q tarteel_realtime tests`
- macOS live-recitation timeline UX checks passed:
  - Red check first failed as expected: `swift test --filter RecitationViewModelTests` failed because repeated `uncertain` and repeated same-ayah `progress` events still created multiple rows.
  - `cd ios/TarteelClientCore && env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test`
  - Result: 28 XCTest tests plus 27 Swift Testing tests.
  - `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_ios_websocket_client tests.test_ios_status_panel -v`
  - Result: 20 tests.
  - `uv run python -m compileall -q tarteel_realtime tests`
  - `git diff --check`
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`
  - `uv run python -B -m unittest discover -s tests -v`
  - Result: 223 tests.
  - `uv run python -B -m json.tool feature_list.json`
  - `uv run python -B -c "import json; data=json.load(open('feature_list.json', encoding='utf-8')); active=[f['id'] for f in data['features'] if f['status']=='in_progress']; print(active); assert len(active) <= 1"`
  - Result: `[]`.
- Modal macOS default and Keychain token persistence checks passed:
  - `cd ios/TarteelClientCore && env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test`
  - Result: 25 XCTest tests plus 27 Swift Testing tests.
  - `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_ios_websocket_client tests.test_ios_status_panel -v`
  - Result: 20 tests.
  - `uv run python -B -m json.tool feature_list.json`
  - `uv run python -B -c "import json; data=json.load(open('feature_list.json', encoding='utf-8')); active=[f['id'] for f in data['features'] if f['status']=='in_progress']; print(active); assert len(active) <= 1"`
  - Result: `[]`.
  - `uv run python -m compileall -q tarteel_realtime tests`
  - `git diff --check`
  - `uv run python -B -m unittest discover -s tests -v`
  - Result: 223 tests.
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`
- macOS native UI polish checks passed:
  - `cd ios/TarteelClientCore && env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test`
  - Result: 44 checks.
  - `uv run python -B -m unittest tests.test_macos_app_project tests.test_ios_recitation_scope_ui tests.test_ios_websocket_client tests.test_ios_status_panel -v`
  - Result: 20 tests.
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`
  - `uv run python -B -m unittest discover -s tests -v`
  - Result: 223 tests.
  - `uv run python -m compileall -q tarteel_realtime tests`
- Focused Modal CUDA-image checks passed:
  - `uv run python -B -m unittest tests.test_modal_serverless -v`
  - Result: 5 tests.
  - `uv run python -m compileall -q deploy tests/test_modal_serverless.py`
  - `git diff --check`
- Post-deploy remote checks passed:
  - `curl -sS https://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run/ping` returned `{"status":"ok"}`.
  - Modal logs since `2026-05-29T16:00:38` show `GET /ping -> 200 OK`.
  - Searching post-deploy logs for `libcublas` returned no results.
- Focused replay fixture checks passed:
  - `uv run python -B -m unittest tests.test_replay_probe tests.test_ws_client tests.test_asr_smoke -v`
  - Result: 22 tests.
- Local fixture load proof passed:
  - `load_replay_audio_file(Path("fixtures/local_audio/108001.wav"), raw_sample_rate_hz=16000)` returned 44.1 kHz mono PCM, 787,968 bytes, about 8.934 seconds.
- Original command shape now passes audio loading:
  - A dummy `ws://127.0.0.1:9/ws/recitation` URL reaches connection setup and fails with connection refused instead of `AudioInputError`.
- Focused Modal prewarm-fix checks passed:
  - `uv run python -B -m unittest tests.test_modal_serverless -v`
  - Result: 5 tests.
- Syntax and whitespace checks passed:
  - `uv run python -m compileall -q deploy tests/test_modal_serverless.py`
  - `git diff --check`
- Focused Modal/replay/backend/Apple source checks passed:
  - `uv run python -B -m unittest tests.test_modal_serverless tests.test_replay_probe tests.test_ws_client tests.test_api tests.test_asr_runtime tests.test_asr_app tests.test_ios_websocket_client tests.test_ios_recitation_scope_ui tests.test_ios_status_panel tests.test_macos_app_project -v`
  - Result: 64 tests.
- Compile check passed:
  - `uv run python -m compileall -q deploy tarteel_realtime tests`
- Swift client core passed:
  - `env CLANG_MODULE_CACHE_PATH=/private/tmp/tarteel-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/tarteel-swift-module-cache swift test`
  - Result: 39 tests total.
- iPhone target build passed:
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototype -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /private/tmp/tarteel-xcode-derived CODE_SIGNING_ALLOWED=NO build`
- macOS target build passed after escalated rerun because sandboxed Xcode/SwiftPM cache access was blocked:
  - `xcodebuild -project ios/TarteelPrototype/TarteelPrototype.xcodeproj -scheme TarteelPrototypeMac -sdk macosx -derivedDataPath /private/tmp/tarteel-xcode-derived-macos CODE_SIGNING_ALLOWED=NO build`
- Full deterministic Python suite passed:
  - `uv run python -B -m unittest discover -s tests -v`
  - Result: 218 tests.
- JSON/active-feature/whitespace checks passed:
  - `uv run python -B -m json.tool feature_list.json` after escalated rerun for uv cache access
  - `uv run python -B -c "import json; data=json.load(open('feature_list.json', encoding='utf-8')); active=[f['id'] for f in data['features'] if f['status']=='in_progress']; print(active); assert len(active) <= 1"` returned `[]`
  - `git diff --check`

## Current Risks

- The Surah 18 fix recovers short-ayah suffix evidence, but it does not improve the CoreML ASR blanks/noise observed during later ayah 4 attempts.
- The Surah 18 manual run was not launched with `--tarteel-capture-audio`, so there is no captured WAV for deterministic replay of that exact mic input.
- Live mic capture/replay is source/test/build verified, but a fresh captured manual WAV has not yet been produced after this slice. The next ASR-quality claim should be based on captured-WAV replay plus unified logs, not memory of a one-off live run.
- CoreML diagnostic logging is source/build/app-replay verified, but a fresh manual microphone recitation has not yet been run after adding the new markers and app replay path.
- CoreML transcript logs are public in local unified logs for developer diagnosis; this is useful for the prototype but is not an acceptable production privacy default.
- The local CoreML path is source/build/runtime-smoke/scored-fixture/app-replay/local-locator verified, but real microphone accuracy, memory use, thermal behavior, and end-to-end UX are still unverified.
- The new macOS app-binary replay uses a fixture audio streamer but does pass through the real `VoiceActivityDetector` and CoreML/local-locator path. It proves app-owned replay mechanics, not the actual microphone streamer cadence. The iOS replay path is now simulator-launched with arguments, but Simulator produces nonfinite model output for this ANE-specialized model and is not success evidence.
- The CoreML local locator/session can now load Tanzil-format `quran-simple-clean.txt` from the app bundle or model directory, and local iPhone/macOS app builds now bundle the ignored full Quran text when present; production packaging/licensing/distribution for that resource is still undecided.
- The Swift local matcher is still an MVP exact/span plus compact-character tolerant locator. It does not fully port the Python locator/session/progression engine, and full-corpus auto-detect performance still needs dedicated tuning.
- The Swift log-mel preprocessing was implemented from the model card and common ASR conventions; scored fixtures now show mixed quality, but this still needs decoder/preprocessing tuning before accepting transcript quality.
- `.models/fastconformer-quran-coreml-streaming` remains ignored and local-only. A fresh checkout or worktree needs that directory or symlink before Apple app builds that include the model resources.
- The post-lock overlap progression fix is locally source/test verified, but the Modal scoped Surah 4 replay has not been rerun against a backend containing this branch.
- The aggregate Modal event mix still needs fresh diagnostics after this second scoped Surah 4 fix; broader ASR noise remains model/audio dependent.
- The new app-icon set is generated from a 512x512 source, so 1024px slots are upsampled; use a native 1024px logo export before production distribution if sharper artwork is needed.
- The new Quran logo is source/build/bundle verified, but exact visual placement has not been screenshot-verified or manually checked in the running iPhone/macOS UI.
- The curated timeline is source/build/test verified, but the running macOS app still needs a visual/live Modal recheck to confirm repeated rows collapse as expected during real ASR streaming.
- The macOS UI polish is source/build verified, but manual visual QA and interaction testing are still outstanding for light/dark mode, drag/drop, diagnostic drag-out, keyboard focus, Settings validation layout, microphone permission, and live backend recording.
- Modal post-deploy `/ping` and scoped Surah 108 replay evidence exists after the CUDA image fix, but the replay produced `lock_candidate` for 108:1 rather than a full lock.
- Modal still needs additional scoped replay, especially the 4:1-3 fixture, plus idle shutdown evidence, cost evidence, and live ASR quality tuning.
- `prewarm` may need rerun after the CUDA image deployment if the model cache Volume is not already hydrated.
- RunPod Serverless remains locally packaged but not live endpoint verified.
- The Apple provider picker was source/build verified, not manually exercised on iPhone or macOS.
- macOS Keychain token persistence needs a manual quit/reopen check with a real token before claiming end-to-end user workflow proof.
- Real ASR quality remains model/audio dependent; this slice only adds provider comparison infrastructure.

## Next Best Step

Run the rebuilt macOS app with live capture, then replay the captured WAV. For the next Surah 18 check, expect `coreml_local_short_ayah_suffix_progress` when ASR emits `في أب` for `18:3`:

```bash
/private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app/Contents/MacOS/TarteelPrototypeMac \
  --tarteel-capture-audio /private/tmp/tarteel-live-surah59.wav

/private/tmp/tarteel-xcode-derived-macos/Build/Products/Debug/TarteelPrototypeMac.app/Contents/MacOS/TarteelPrototypeMac \
  --tarteel-replay-audio /private/tmp/tarteel-live-surah59.wav \
  --tarteel-replay-surah 59
```

Compare unified logs for `coreml_asr_audio_capture_*`, `coreml_asr_audio_window`, `coreml_asr_blank`, `coreml_asr_transcript`, and `coreml_asr_locator_event`. If live capture and replay differ, investigate app lifecycle/capture timing. If both are bad with healthy RMS/peak, tune CoreML preprocessing/decoder/model behavior. If both are bad with weak RMS/peak or high near-silence, tune mic capture/gain/conversion.

Install and run the CoreML preset on a physical iPhone with selected Surah 108, then recite Al-Kawthar and capture either `coreml_asr_transcript` progress/lock evidence or `coreml_asr_invalid_output` device logs. Simulator evidence is no longer the iOS acceptance gate for this model.

After that manual E2E proof, decide whether the Swift local path should keep the lightweight matcher with selected-Surah scoping or port the Python locator/session/progression engine, then use the scored fixture gate to tune the `108002` word-boundary/fusion failure without regressing `108003`.

Open the rebuilt iPhone and macOS apps and visually confirm the Quran logo sizing and placement in the home/recitation headers, ideally in light and dark appearances.

Reopen the rebuilt macOS app, record against Modal again, and confirm the right panel says `Timeline` and repeated `uncertain` / same-ayah `progress` rows collapse into one row with an `xN` badge. Also confirm first-launch Custom/Modal defaults, Keychain token reload, light/dark mode, `Space`/`Command-R`, `Command-F`, Surah filtering, Settings validation feedback, microphone permission, and a local or Modal `/ws/recitation` recording.

Run the remaining Modal replay or recitation proof, especially scope 4:1-3,
then check logs after the test window. For this branch, confirm the scoped
Surah 4 trace has seq 7 `locked` at `next_expected_ref=4:1:10` and seq 9
`progress` at `next_expected_ref=4:1:11`. Replay command:

```bash
uv run --with websockets python -m tarteel_realtime.replay_probe \
  --url 'wss://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run/ws/recitation' \
  --scope 108 \
  --audio-path fixtures/local_audio/108001.wav \
  --chunk-ms 1000 \
  --bearer-token '<token>' \
  --disable-ping \
  --include-events

uv run --with websockets python -m tarteel_realtime.replay_probe \
  --url 'wss://moabdelmoez--tarteel-realtime-asr-fastapi-app.modal.run/ws/recitation' \
  --scope '4:1-3' \
  --audio-path fixtures/local_audio/004001.wav \
  --chunk-ms 1000 \
  --bearer-token '<token>' \
  --disable-ping \
  --include-events
```

If the first post-deploy request has to hydrate model cache again, rerun:

```bash
uvx modal run deploy/modal_asr_app.py::prewarm
```

Repeat the same replay probe against RunPod once Modal is clean so the provider
comparison uses the same evidence shape.
